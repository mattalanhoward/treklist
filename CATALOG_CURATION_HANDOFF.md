# Catalog Curation Handoff

_Last updated: 2026-07-08. Working DB: `treklist_local` (Mongo via `server/.env` `MONGO_URI` — ⚠ the URI's default path db is PROD `treklist`; the app overrides via mongoose `dbName`, but raw scripts/mongodump must explicitly `.useDb("treklist_local")` / swap the URI path). All work is LOCAL data. Code was on branch `feature/catalog-curation`, which has since been **merged to `main` via PR #350** — `main` is now the only long-lived branch and the current working branch (see [[barcode-and-ai-rework]])._

## ▶▶ START HERE (2026-07-08) — resume brand-by-brand additions

**Catalog state:** De-consolidation onto the locked variant standard is COMPLETE (0 violations catalog-wide) and a full accuracy QA pass is COMPLETE. **3,046 active items** as of 2026-07-09. Recent: LiteAF 21, Bonfus 30, Altra 40 shoes + 1 gaiter, Black Diamond 120 hard goods + 190 apparel (2026-07-08); Feathered Friends 74, Arc'teryx 126 & Patagonia 31 apparel (resold via FF), **Hilleberg 43 tents** (2026-07-09). Everything below "▶▶ OSPREY" in this file is the historical per-brand log — read it when you need detail on how a specific brand was done, but it's not where to start.

**⚠️ RESOLD-OFFER ITEMS TO RE-POINT (2026-07-09):** the **Arc'teryx (126) + Patagonia (31)** apparel items (`create-ff-resold-apparel.js`) are real catalog items with `brand` = the actual brand, but their MerchantOffer is **Feathered Friends** (`merchantId:"direct-featheredfriends"`, FF product-page deepLinks) because that's the only place we can currently link them. **When Arc'teryx/Patagonia direct or affiliate access lands, re-point these offers** to the brand's own store. Find them: offers with `merchantId=="direct-featheredfriends"` on a CatalogItem whose brand is Arc'teryx/Patagonia (brand≠retailer = the signal; no special flag). Weights (152/157) pulled from body_html ('X.X oz (NNN g)' in the features list) via `backfill-ff-resold-weights.js` + a reference-size disclaimer (FF states no size → generic honest wording, not an invented 'size M'); ~5 lacked a weight in the copy. `create-ff-resold-apparel.js` now parses weight inline too.

**⚠️ SCHEMA CHANGE (2026-07-08) — 3 new itemTypes added to `attributeSchemas.js` (SCHEMAS is the single source of truth; the enum + `/api/admin/catalog-items/item-types` + admin dropdown all derive from it):** **`Gaiters`** (leg/shoe) and **`Neck Gaiter`** — split out of the catch-all `Hat/Headwear` (a leg gaiter and a neck tube are very different garments); and **`Softshell Jacket`** — non-waterproof stretch/wind shells that aren't Rain Jacket/Fleece/Insulation. Each was wired through `inferItemType.js` (classifier rule + gendered `CATEGORY_BY_ITEM_TYPE`), client `catalogTaxonomy.js` (new "Gaiters" subcategory under all 3 clothing categories; "Neck Gaiter" added to Men's/Women's), `attributeLabels.js`, and all 6 locale `common.json` files (itemType + subcategory translations). **These ship to prod when the catalog migrates** (schema/code change, inert on existing data). Existing gaiters backfilled: `backfill-gaiter-types.js` moved 23 leg gaiters → Gaiters, 4 neck gaiters (Buff/SIMOND/Zpacks/FarPointe) → Neck Gaiter, fixed 1 mistyped OR balaclava, demoted the Zpacks "Spare Gaiter Cord" to Other.

**The standing rule for every new brand (LOCKED 2026-07-03, do not relitigate):** the spec that defines what the item *is* (volume/temperature/R-value/occupancy) → its own product name/CatalogItem. The spec that defines how it *fits* (torso/length/fabric) → a variant axis. Color is never a variant; Men's/Women's are always separate products. Full rule + edge cases in the "REI model" section search below, or ask for `feedback-catalog-variant-standard` from memory.

**Buy-link pattern per brand (LOCKED 2026-07-03):** check whether picking a size/variant on the brand's own site changes the URL. If yes → set a `deepLink` per variant. If the brand uses one page with an on-page selector (most Shopify stores, e.g. HMG/Zpacks/Sea to Summit) → one item-level MerchantOffer, no per-variant links.

**Standard pipeline for a new Shopify/Woo brand:**
1. Probe the feed: `curl <domain>/products.json` (Shopify) or `<domain>/wp-json/wc/store/v1/products` (Woo). If Cloudflare-blocked and not Shopify/Woo, it needs an affiliate network (AvantLink/Impact — none currently available) or manual copy-paste (see the Osprey pattern below for that flow), or a per-page scrape (see Exped/FarPointe patterns below).
2. Import: `ingest-shopify-catalog.js --domain <domain> --brand "<Brand>" --merchant-name "<Brand>" --commit` (direct/unmonetized offers). ⚠ Cloudflare sometimes TRIMS the top-level `/products.json` list — verify count via a per-collection/vendor curl with a UA header if the top-level count looks low (bit us on GGG).
3. Type: `ai-type-untyped.js --brand <Brand> --commit`.
4. Curate with a one-off `curate-<brand>.js`: archive noise (parts, gift cards, resold 3rd-party care products, MTO/blem/custom, car-camping), dedupe repeat listings, fix AI mistypes, backfill categories, consolidate variants per the standard above.
5. Verify: active count, 0 items without an offer, spot-check a few typings.
6. Log what you did in this file's brand-pipeline section below (new entry, same format as existing ones) — this file is the durable record.

**Default decisions to apply WITHOUT asking** (user delegated this 2026-07-01, [[feedback-autonomous-brand-curation]] in memory has full detail): archive parts/O-rings/gift-cards/service-products/resold-care-products/custom-MTO/car-camping noise; archive peripheral apparel on hardware brands but keep core technical apparel; trust per-variant weights that genuinely vary, null obvious placeholder weights (flag a TODO instead of fabricating); brand-direct unmonetized offers when no affiliate access. **Only pause to ask on genuinely high-impact ambiguity**, or anything that would overwrite/replace already-curated brand data (existing curated items always take precedence — never re-import over a hand-curated brand).

**⚠️ Critical gotcha:** never call `.save()` on a `CatalogItem` doc loaded with `.select()` — the pre-save hook re-normalizes every field from `this.*` and silently blanks anything not selected (caused a real data-loss incident 2026-06-29). Use `CatalogItem.collection.updateOne({ _id }, { $set: {...} })` for field patches instead.

**Next brands to pick up (from the backlog — none of these are started yet):**
| Brand | Notes |
|---|---|
| Smartwool | ✅ Shopify open feed at `/en-us/products.json` (probed 2026-07-09) — merino base layers/socks; apparel/sock-heavy, needs filtering. IMPORTABLE, not started |
| Icebreaker | merino apparel |
| Gregory | ❌ Vue SPA, no open feed (probed 2026-07-09) — scrape/affiliate |
| Mountain Hardwear | ❌ CONFIRMED no open feed 2026-07-09 (Demandware/SFCC + PerimeterX). Affiliate program = **CJ (Commission Junction)**, not AvantLink/Impact → needs a CJ account or manual. Details in the no-feed table below. |
| Neve Gear | AU multi-brand retailer, unconfirmed platform |
| Kiwi Ultralight | ❌ Wix/Ecwid, no standard feed (probed 2026-07-09) |
| Marmot | ❌ Cloudflare-blocked, no open feed (probed 2026-07-09) |
| GramXpert | ✅ WooCommerce Store API open (probed 2026-07-09) — 46 EU cottage UL items. IMPORTABLE, not started |
| GramXpert | EU cottage |
| Deuter | enterprise nginx, likely no open feed; existing catalog Deuter daypacks are manual entries w/ no variants — revisit those too |
| Nitecore | headlamps/power — site is mostly unrelated flashlight/vape gear, heavy filtering needed |
| Flextail | pumps, small line |
| Katadyn | filters — group site also has Optimus/Trek'n Eat, needs filtering |
| Snow Peak | current catalog items are ALL manual Amazon entries → REPLACE entirely from snowpeak.com once a feed is confirmed |
| TOAKS | current catalog items are ALL manual Amazon entries → REPLACE entirely from toaksoutdoor.com once a feed is confirmed |
| Jack Wolfskin | jack-wolfskin.com — Salesforce Commerce Cloud (Demandware), same blocker as Osprey/Mountain Hardwear/Hoka → no open feed, needs affiliate or manual; not started |
| Decathlon (expand existing) | not a new brand — already in catalog via `ingest-decathlon-awin.js`/Awin, but variant coverage is incomplete; user wants all variants included. Also still owes a dedicated REI-rule variant pass (see Open QA flags below, alongside SIMOND/QUECHUA/FORCLAZ) |

No-feed brands that need affiliate access or a scrape decision the user hasn't greenlit yet (don't start without checking in): Hoka, Kahtoola, Granite Gear, Petzl (partially covered by Black Diamond headlamps). Osprey has 4 remaining models blocked on the user pasting specs (Kyte LT, Sportlite 25L, Hikelite LT 30, Kestrel 38L) — see the "▶▶ OSPREY" section immediately below for the exact resume point.

**Open QA flags (not blockers, just not yet resolved — full list in `server/reports/catalog-qa-2026-07-04.md`):** Zenbivy has both "Overland ZipBed" and "ZipBed Overland" live (likely a dup, consider archiving one); Katabatic Flex 40°F variant weight matches the brand's own 30°F row (their feed's error, not ours); SIMOND "10°c MT900" listed twice at 700g/630g (likely a Decathlon size mismatch); ~10 items have genuinely unpublished weights (Dandee ×3, Makalu III, MT500 Twinnable ×2, X-Dome Pro 1+, MSR Snowshoe Carry Pack, TaR Synergy/Trekker, Gossamer SitLight, Vargo ExoTi). SIMOND/QUECHUA/FORCLAZ/Decathlon haven't had a dedicated REI-rule variant pass yet (large item count). Cumulus/EE bags need variants not obtainable from the GGG/Farlite back-doors (would need direct-source work).

> 🔧 **Admin editor redesign brief:** `ADMIN_CATALOG_REDESIGN_HANDOFF.md` (repo root) — tabbed catalog editor + a **Variants tab** (no way to edit per-variant weights today; blocks e.g. Nemo weight entry). Feed it to a planning model, then build.

## ▶▶ OSPREY — ⏸ PAUSED 2026-07-01 (user taking a break from manual entry; resume here)
**RESUME = the 4 packs in "NEXT" below: Kyte LT (multi-vol, need spec + per-vol ASINs) + singles Sportlite 25L / Hikelite LT 30 / Kestrel 38L (need spec pastes). Everything else done. Scripts + ledger all ready.**
_(Aside 2026-07-01: added one-off **Kula Cloth** (Toiletry, direct buy-link) via `create-kula.js` — unrelated to Osprey. ⚠ Toiletry has empty attribute schema → pre-save hook wipes freeform attrs; set them via `updateOne` with attributes-only $set, NO itemType.)_

**Manually-entered Osprey packs (brand "Osprey", NO itemGroupId — match by `brand:/osprey/i`). 31 → 16 + Aura AG (new) = 17 active.**
- **DONE earlier:** stripped "Men's/Women's" + "Osprey " prefix from names → set `gender` attribute. Consolidated 9 models into a **"Size" axis = VOLUME** (e.g. Atmos AG = Size[50L/65L]). `curate-osprey-sizes.js`.
- **THE ACTIVE TASK:** add **Torso-Size variants** to each pack. Final structure = **Volume × Torso Size** (multi-volume) or **Torso Size only** (singles). Rename existing "Size" axis → **"Volume"**, add **"Torso Size"** axis. Per-variant `volumeLiters` + `weightGrams` + `torsoFitRange` (+ `loadCapacityKg` when published) — **all swap per torso size** via variant.attributes merge (volume varies by torso, e.g. Atmos 65 = 65L S/M, 68L L/XL). torsoFitRange = the size label (no inch ranges in pastes; user-confirmed). Clean attrs only — AntiGravity = **`frameType: "Internal Frame"`** (the manual items' "Anti-Gravity" value FAILS Backpack validation).
- **SCRIPTS (both new this session, `--commit` to apply, idempotent):**
  - `curate-osprey-torso.js` — **enriches existing** items. Config block per model in `MODELS`: rows accept `lbs` OR `g` (metric); load `loadLb` OR `loadKg` OR omit; optional `from` = current name to **rename** (e.g. "Exos 48" → "Exos"); optional `defaultKey`. Handles 1–3 volumes (single-volume omits the Volume axis → Torso-only). Sets `$locals.lenientAttributes`. ⚠ a renamed model's `from` won't be found on RE-RUN (already renamed → harmless "not found" warning).
  - `create-osprey-aura.js` — **creates NEW** items not yet in catalog. **Fetches images LIVE from the Amazon Creators API** per ASIN (env `MOCK=0`, works locally) to seed imageUrls (the daily `amazonImageRefresh` job only refreshes items that ALREADY have an Amazon image, so seeding is required) + creates one Amazon MerchantOffer. Refuses to create without images. Skips models that already exist.
- **DEFAULT-VARIANT RULE:** default to the torso whose volume = the **nominal/ASIN volume** (men's S/M = nominal; women's **WM/L = nominal**, WXS/S is smaller). Keeps base volume coherent with the canonical ASIN.
- **⚠️ 1-ASIN-per-item decision (2026-06-30):** offers + canonical ASIN **can't route per-variant**, so consolidated multi-volume items keep **ONE ASIN** (not split per volume). See memory [[project-per-variant-offer-routing]] — **revisit ~2026-07-07** to add variantKey-aware offers + per-variant images, then attach the other ASINs. Aura AG built on 50L ASIN `B09JXJ9CM4`; 65L `B09JXJVDRT` in the ledger.
- **🗂 ASIN LEDGER (regenerated 2026-07-02):** `CATALOG_ASIN_LEDGER.json` (repo root) = durable backup of all **194** Amazon-linked items + per-volume ASINs, regenerated by `build-asin-ledger.js` (was 214; dropped ~20 Amazon items replaced by brand-direct — TAR pads, MSR, Platypus, Nemo pads, Opinel N°06) (extra per-volume ASINs hand-kept in its `EXTRA_ASINS` map). Replay it when the variant-offer schema lands. **13 multi-volume Osprey packs, EVERY volume's ASIN captured — 0 pending.** **PROCESS:** going forward, when the user pastes a multi-volume pack, get **all** per-volume ASINs → one canonical in DB, rest into `EXTRA_ASINS`, then re-run the ledger.
- **⚠️ VOLUME-LEVEL LINKS ONLY (user, 2026-07-01):** do **not** track per-torso-size or per-color ASINs. One ASIN per volume (may be a specific Amazon size/color child, used as the volume's buy-link). Future variant-offer routing = per **VOLUME**, not per torso/color — simpler than full variantKey routing.
- **📦 Backup 2026-07-01:** fresh full `mongodump` at `server/backups/dump-treklist_local-20260701T083935/` (catalogitems 1110, merchantoffers 1164, gearitems 240, globalitems 160). ⚠️ Mongo URI's default db = prod `treklist`; for mongodump swap the URI path to `/treklist_local` (the app overrides via mongoose `dbName`, but mongodump reads the URI).
- **⚠️ CANNOT fetch osprey.com** (Cloudflare 403s curl AND WebFetch). **User copy-pastes the spec block per pack.** Manual item pattern = Amazon images (`m.media-amazon...`) + one MerchantOffer (`network:"amazon"`, `merchantId:"amazon-us"`, `externalProductId`=ASIN, `deepLink` `/dp/<ASIN>?tag=treklistapp-20`), `createdBy` = `69565d7c3480c2216f915a36`.
- **✅ DONE (15):**
  - **Atmos AG** — Volume[50/65]×Torso[S/M,L/XL], 1960–2180g, load 15.9/18.1kg.
  - **Atmos AG LT** — same axes, 1815–1925g, no load (blank in source).
  - **Aura AG** — NEW (create-osprey-aura), Womens, Volume[50/65]×Torso[WXS/S,WM/L], 1888–2085g, load 16/18kg, 5 imgs, ASIN 50L `B09JXJ9CM4`. Default = 50L/WM/L.
  - **Exos** — renamed from "Exos 48", Volume[38/48/58]×Torso[S/M,L/XL], 1222–1334g, load 11/14/16kg. ASIN `B0DS6LN8C5`, default 48L/S/M.
  - **Eja** — renamed from "Eja 48", Volume[38/48/58]×Torso[WXS/S,WM/L], 1178–1280g, load 11/14/16kg. ASIN `B0DS6MK66Y`, default 48L/WM/L. _(has 2 offers — pre-existing, check in an offer-integrity sweep.)_
  - **Exos Pro 55** — Torso-only[S/M=55L,L/XL=58L], NanoFly, **979g flat** (⚠ no per-torso weight published), load 18kg. ASIN `B0BTTQRJTL`.
  - **Eja Pro 55** — renamed from mis-named "Eja Pro 48" (it's the 55L pack), Torso-only[WXS/S=52L,WM/L=55L], NanoFly, **934g flat**, load 18kg. ASIN `B0BTTRJJ88`.
  - **Aura AG LT** — NEW (create-osprey-aura), Womens, Volume[50/65]×Torso[WXS/S,WM/L], 1720–1820g, no load (blank), no dims, 6 imgs. Canonical = **65L** ASIN `B0BKQJH4Q2` (user gave 65L; 50L ASIN = TBD), default 65L/WM/L.
  - **Talon** — `curate-osprey-flat.js`. RAGGED → single **Size** axis [22L,26L,33L,44L (S/M),44L (L/XL)], 1070–1620g, load 13.6/15.9kg. Mens. Canonical swapped to 22L ASIN `B0DSCN3TZC` (+8 refetched imgs). 4 per-vol ASINs in ledger.
  - **Tempest** — `curate-osprey-flat.js`. All one-size → **Volume** axis [22L,26L,33L] (NO torso, no 44 — discontinued), 1020–1200g, load 13.6kg. Womens. Canonical `B0DSCQF6QH` (=22L). 3 per-vol ASINs in ledger.
  - **Stratos** — `curate-osprey-flat.js`. All O/S → **Volume** [24/34/36/44], 1340–1660g, load 13.6/16kg. Mens. **Daypack→Backpack** (44L > Daypack max 40) + subcat "Backpacking Packs". Canonical→24L `B0G6GD7JLS` (+17 imgs). rainCover. 4 ASINs in ledger.
  - **Sirrus** — same as Stratos, Womens, 1300–1600g. Canonical→24L `B0G6GKWX8D` (+17 imgs). All 4 ASINs in ledger (34L `B0G6GKJMWH` supplied later).
  - **Hikelite** — `curate-osprey-flat.js`. **Daypack** (all ≤40L), **Volume** [18/26/28/32], 730–1030g, load 9/11kg, Unisex. Per-variant `hipBeltType` (18/26 Webbing Only, 28/32 Padded). Canonical→18L `B0FGY23CXN` (+16 imgs). ⚠ Daypack schema strips backPanelType/mainFabric — only Daypack-valid attrs set. 4 ASINs in ledger.
  - **Talon Velocity** — `curate-osprey-torso.js`. **Daypack**, full Volume[20/30]×Torso[S/M,L/XL], 870–1000g, NO load (blank). Mens, Frameless (running-vest). Canonical→20L `B0CGQ3P6WB` (+14 imgs). 2 ASINs in ledger. _(torso script now also swaps canonicalAsin + refetches images + sets itemType.)_
  - **Tempest Velocity** — NEW (create-osprey-aura, now parameterized itemType/subcat), **Daypack**, Volume[20/30]×Torso[WXS/S,WM/L], 820–960g, no load, Womens, Frameless. Canonical 20L `B0CGQ3PDB1` (+14 imgs) + offer. 2 ASINs in ledger.
- **⚠ RAGGED / one-size packs → `curate-osprey-flat.js`** (single explicit-variant axis), NOT the torso matrix: use when only SOME volumes split by torso (Talon) or all are O/S (Tempest). The 2-axis UI can't disable invalid combos. `axisName` "Size" (combined vol+torso) or "Volume" (pure). Can swap `canonicalAsin` + refetch images.
- **NEXT — remaining 4:** need spec pastes + (multi-volume) all per-volume ASINs. **Kyte LT** (28/35, Backpack, Womens); singles → flat = **Sportlite 25L, Hikelite LT 30, Kestrel 38L**. ⚠ Daypacks: `frameType` enum is `Frameless/Internal Frame/Foam Back`; Daypack schema strips backPanelType/mainFabric/torsoFitRange (validateAttributes → only schema fields survive); volumeLiters max 40 — if any volume >40, switch to Backpack (as Stratos/Sirrus).
- **Active Osprey now 19** (16 + Aura AG + Aura AG LT + Tempest Velocity; renames don't change count).

## BRAND PIPELINE — to add (researched 2026-06-29)
Goal: make the catalog very complete. **Curated so far (done): Hyberg, Durston, Atom, Zpacks, Atelier, HMG, Nashville Pack, Dandee Packs, Hilltop Packs, Vargo (72; weights TODO), Katabatic (17; Flex consolidated), Cascade family = MSR/TAR/Platypus/SealLine/PackTowl (typing-review ✅ 2026-07-02: MSR "Backpack"→Backpacking Tent + deduped, TAR Honcho Poncho→Quilt + kids/dups archived), Zenbivy (77), Gossamer Gear (86; ✅ user-cleaned 2026-07-02), Sam Bob (14; curated + 14-cell size matrix on tops), Sea to Summit (195), Outdoor Research (502), CNOC (21), Nemo (49; done for now), CNOC (8; consolidated+weighted), Opinel (10 stainless knives; weighted), Kula Cloth, Diorite Gear (1 pole), Cumulus (9 active; 10 GGG + 6 Farlite gap fills → **consolidated 2026-07-03** into 4 Fill-Weight families [Neo Quilt/The Quilt 150/300/450, Aerial 180/250/330, Vencer 100/200] + 5 singletons [Magic 100/Panyam 600/Spotter 600/X-Lite 400/Teneqa 1000]; `cumulus-consolidate.js`; per-variant links mix GGG+Farlite URLs), Tarptent (6: 5 added via GGG + Rainbow pre-existing), Western Mountaineering (5: 3 bags + quilt + liner; via GGG), Six Moon Designs (7 shelters; via GGG), Alpenglow Gear (1 pump; via GGG), Pa'lante (9 packs; direct Shopify), FarPointe (13 apparel; direct Squarespace), Exped (235 full catalog; direct Nuxt/Drupal scrape), Enlightened Equipment (7 quilts+pillow; via Farlite), Big Agnes (116; direct Shopify, de-consolidated to REI standard, QA'd), LiteAF (21; direct WooCommerce, targeted create), Bonfus (30; direct WooCommerce, targeted create), Altra (40 trail/hiking shoes + Trail Gaiter; direct Shopify, targeted create, scoped from 315 shoe listings), Black Diamond (120 hard goods + 190 apparel = 308; direct Shopify config-driven create — headlamps/lanterns/poles/packs/tents/gaiters + user-requested climbing gear (helmets/harnesses/crampons/ice-axes as Other) + full apparel pass gendered), Feathered Friends (74; direct Shopify vendor-filtered — premium down bags temp-split per REI standard + quilts + down jackets; real weights from body_html spec tables), Arc'teryx (126) + Patagonia (31) apparel (RESOLD via FF — offers point at Feathered Friends until we get brand-direct access; typed by FF garment tags), Hilleberg (43 tents; direct Nuxt-SSR scrape — full line parsed from the __NUXT_DATA__ product JSON, capacity/weight/season/type/fabric).**

**🧹 Stragglers sweep — ✅ 2026-07-02 (`curate-stragglers.js`):** MSR "Backpack"×24→Backpacking Tent (+archived non-tent parts: ferrule kit/repair splints/gear shed, and deduped exact-name Hubba/FreeLite/Access/Remote dups); TAR Honcho Poncho (mis-typed Rain Poncho)→**Quilt** + archived kids/past-season + deduped; **all 15 no-category items given a category** (Zpacks/Vargo/Atom/OR/CNOC accessories → Footwear/Accessories&Tools/Kitchen/Health per name); archived Sea to Summit "Carbon Neutral Order" (checkout line item, not a product); brand casing **UGreen→Ugreen**; dropped Osprey **Eja**'s stray unmonetized bever.nl offer (kept Amazon). **Active 1889→1869.** ℹ️ Remaining **28 itemType:"Other"** are legit accessories with NO matching taxonomy type (trowels/pumps/whistles/traction cleats/booties/gaiters/cases) — "Other" is correct for them, all now categorized. ⚠️ **INCIDENT (caught+recovered):** first pass used `.save()` on projected (`.select()`) docs for the retypes → pre-save normalize hook wiped `brand`→"undefined" on 20 docs; restored catalogitems from the 12:52 mongodump and re-ran with `updateOne` $set. Reconfirms [[gotcha_select_save_wipes_fields]] — NEVER `.save()` a projected doc.

**⚠ Whole Shopify queue DONE 2026-07-01. Osprey ⏸ paused (15/19). One-off: Kula Cloth.**

### 🆕 13 brands researched 2026-07-01 (feasibility probed — none imported yet)
**✅ Open Shopify feed (`/products.json` works even behind Cloudflare CDN) — same toolchain as HMG/Nashville/Hilltop; direct unmonetized buy-links; no affiliate needed:**
| Brand | Domain | Feed | Catalog / relevance | Effort |
|---|---|---|---|---|
| **Vargo** | vargooutdoors.com | ✅ **DONE 2026-07-01** | Ingested 111 (`ingest-shopify-catalog --brand Vargo --merchant-name Vargo`, direct offers) → AI-typed → `curate-vargo.js` archived **30** noise (parts/O-rings/lids/hangers/carabiners/whistles/lighter/wallet/cozies/car-camping grills+fire pit), fixed mistypes (**Sobata + Fire Box are STOVES**, were Pocket Knife/Other), backfilled categories, deduped the manual Long-Handle Spoon. Then **archived 9 apparel** (user, weightless & peripheral). **72 active, all have offers.** ⚠️ **WEIGHTS LEFT BLANK (user, 2026-07-01):** Vargo feed grams are unreliable (placeholder 227g=8oz across unrelated items; NOT in body_html — only in JS specs) → nulled 9 placeholders; **~20 weightless + ~50 unverified feed weights** = **TODO: dedicated Vargo weight-lookup pass** (page-scrape JS specs or manual). ⚠️ spot-check odd typings (e.g. 4 "Backpacking Tent" — Vargo makes no tents). | Low-Med |
| **Katabatic Gear** | katabaticgear.com | ✅ **DONE 2026-07-01** | Ingest (`--brand Katabatic --merchant-name Katabatic`) → AI-type → `curate-katabatic.js`. **21 active, all offers, CLEAN feed (real per-variant quilt weights — no weight fix).** Elite Alsek/Chisos/Grenadier/Palisade/Sawatch (distinct models) w/ **Size×Fill variants**; 4 down jackets (Insulated Jacket, deduped 7→4 dup listings); 3 bivies (Tarp Shelter); 2 windshells (Rain Jacket); 2 down hoods (retyped Quilt→Hat/Headwear). Archived 17 (services/parts/resold Gear Aid care products/dup listings). **CONSOLIDATED 2026-07-01** (`consolidate-katabatic-flex.js`, Zpacks precedent): the 5 "Flex N°F Quilt" items → **ONE "Flex Quilt" with Temperature[5]×Size[6]×Fill[2] = 60 variants** (per-variant weight + tempRatingF/C + fillPower), default 15°F/6'/900fp. **"Shells" fill option dropped from ALL Katabatic quilts** (Flex + 5 Elite) 2026-07-01 (`drop-katabatic-shells.js` — bare-shell/DIY-fill, not gear-relevant): Fill now [850fp/900fp]; Flex 90→60, each Elite 18→12 var. **17 active now.** ⚠️ 5 temp URLs collapse to one buy-link (per-variant offer limit) — logged for future: 5°F=`flex-5-f-quilt`, 15°F(canon)=`flex-15-ultralight-quilt`, 22°F=`flex-22-ultralight-quilt`, 30°F=`flex-30-ultralight-quilt`, 40°F=`flex-40-f`. Elite models NOT consolidated (each a distinct named model). | Low |
| **Zenbivy** | zenbivy.com | ✅ **DONE 2026-07-01** | Used **zenbivy.com (US, USD)** not .eu. Ingest 82 → AI-type → archived 5 noise (repair/patch kits, compression caps, 2 "Custom Item" services). **77 active, all offers:** 33 Quilt + 15 Sleeping Bag (bed/quilt+sheet systems), 10 Pillow, 8 Dry Sack, 6+3 pads, 1 mug. ~31 weightless components (sheets etc.) — acceptable. |
| **Gossamer Gear** | gossamergear.com | ✅ **DONE 2026-07-01** | Ingest 129 (all vendor "Gossamer Gear", incl. mislabeled resold) → AI-type → `curate-gossamer.js`. **86 active, all offers.** Archived 47: 3 pre-existing manual pack dups (Gorilla 50/Mariposa 60/Mirage 40 — feed variant versions supersede), 7 resold third-party (Toaks/DAC/Nitecore) + dup Kula Cloth + 2 Starter Kit bundles, 34 "Other" (parts/bundles/accessories/resold care). **✅ user-reviewed & DONE 2026-07-02** (86 active — user cleaned it up). Keeps GG packs/tents/dry bags. |
| **Sam Bob** | sambob.biz | ✅ **DONE 2026-07-01** | Cottage Alpha-Direct fleece apparel. Ingest 101 → AI-type (Fleece/Insulated Jacket) → archived 32 MTO/custom/artist-series/joggers, then **deduped 43 redundant same-name listings** (e.g. "Alpha 90 Hoodie" ×17 — per-colorway dup products, color-collapsed to 1). **26 active** = distinct Alpha 60/90/120 + Microgrid/Octa/PowerGrid hoodies & crewnecks + Basin sun hoodie + Flowfold fanny. All offers. | **FURTHER CURATED 2026-07-02** (`curate-sambob.js`): archived 5 more (dup listings with bogus box-weights 3175/5443g + stray untyped 'Alpha 90'), standardized all Alpha/Octa/Microgrid hoodies+crewnecks to **Fleece Jacket**, added **`fleeceType`** (added 'Alpha Direct' to the Fleece Jacket enum; Alpha/Octa→Alpha Direct, Microgrid→Grid Fleece). **15 active.** ⚠ open: 'SAMBOB crewneck' fabric ambiguous (fleeceType unset), 2 weightless (Alpha 190 Hoodies, Microgrid Crewneck), 'SAMBOB five panel' is merch (keep?). |
| **Sea to Summit** | seatosummit.com | ✅ **DONE 2026-07-01** | 624 products but **389 were "Like New"/"Spare Parts"** (added those to `ingest-shopify-catalog.js` JUNK_TYPE) + 16 title-excluded past-season → 218 kept (99% w/ weights). AI-type → archived 38 Other/Wallet (buckles/straps/kitchen-sinks/RFID wallets/camp seats/repair kits/car-camping). **195 active, all offers:** 34 Dry Bag, 33 Utensil, 23 Pot, 16 Sleeping Bag, 14 Pad, 11 Liner, 10 Pillow, 7 Towel/Head Net/Tent, 6 Mug/Toiletry, etc. |
| **Outdoor Research** | outdoorresearch.com | ✅ **DONE 2026-07-01** (user chose "core + technical apparel") | Ingest 518 → AI-type (apparel maps cleanly to gear types) → archived 16 non-gear Other (candles, Nikwax care, suspenders, CarryOut duffels, blanket, shipping-protection). **502 active, all offers:** 113 Gloves, 90 Headwear, 50 Insulated Jacket, 45 Fleece, 50 Hiking Pants, 39 Hiking Shirt, 33 Shorts, 28 Rain Jacket, 16 Rain Pants, + gaiters/base layers/etc. ⚠️ **502 is large** (more than the ~400 estimate — OR apparel nearly all maps to gear types). If it dominates the catalog, a lifestyle-vs-technical trim within Hat/Shirt/Pants is a future review (needs per-item judgment). **WEIGHTS backfilled 2026-07-01** (`curate-or-weights.js`): ingest came in 87% weightless because it **drops weight when all size-variants share ONE weight** (guard for configurable no-weight products) — OR's feed does that for most apparel. Backfilled from feed for items ≤400 g (gloves/hats/base layers/shirts — packaging negligible = real weight); left **112 heavy jackets/parkas blank** (feed weight is SHIPPING weight, ~2× real, e.g. Snowcrew Down 1151 g vs ~570 g). **Now 78% weighted (390/502).** Heavy items need real spec-page weights (JS scrape) if wanted. |
- **⚠️ INGEST GOTCHA (general):** `ingest-shopify-catalog.js` sets `weightGrams=undefined` when all size-variants share one weight (`weighted.length !== 1` at ~line 330). Fine for configurable packs, but **apparel with a uniform weight across sizes loses its weight**. Affects OR (fixed → 78%) + **Sam Bob (fixed → 22/26)**; Cascade apparel is tiny (MSR 3, Platypus 1 — left). `curate-or-weights.js` (feed uniform/median weight, ≤400 g guard) is the reusable backfill for apparel brands.
- **GENDER (2026-07-01, `backfill-gender.js`):** decision — men's/women's stay **SEPARATE items** (gender = attribute + gendered category, NOT a variant axis; matches OR's 500+ apparel + the schema). Set `attributes.gender` (Mens/Womens) on **357** gendered items catalog-wide + fixed **70** mis-categorized "Unisex Clothing" → gendered. ⚠ Root cause: `inferItemType.js` gender regex only matched straight `'`, not curly `’` (Katabatic) — **fixed** (`[’']` class) so future ingests categorize gendered apparel right.

**🌟 Cascade Designs family — ✅ DONE 2026-07-01** (cascadedesigns.com — ONE open Shopify store, 368, 5 owned brands). Ingest (vendor→brand, merchant "Cascade Designs", direct offers) → AI-type per brand → `curate-cascade.js`. **252 active, all offers:** MSR 156, Therm-a-Rest 58, Platypus 24, SealLine 9, PackTowl 5.
- **REPLACED 11 Amazon-linked items (archived; brand-direct supersedes):** TAR NeoAir Xlite NXT -RS/-R/-RW/-L + XTherm NXT R/RW/L (7 → now consolidated Size-variant pads `NeoAir XLite/XTherm NXT`), MSR Groundhog + Mini-Groundhog Stakes + PocketRocket 2 (3), Platypus Quickdraw (1). Also archived 2 HMG-resold dups (TAR NeoAir, Platypus Hoser).
- **Archived 116 "Other":** MSR ~97 = **entire snowshoe line (~40) + bindings + snow tools + replacement parts** (out of backpacking scope, no snowshoe itemType — ⚠️ REVIEW: reversible if user wants snowshoes); TAR 13 + Platypus 6 = parts/accessories.
- ⚠️ **TYPING REVIEW pass needed** (AI mistypes): TAR "Rain Poncho"×7 (likely tarps/blankets), Platypus "Hat/Headwear"×3 (bottle caps?), MSR "Backpack"×24 (MSR makes few packs — likely tent bodies/stuff sacks), PackTowl "Rain Poncho"×1, SealLine "Hiking Shirt"/"Document". Core gear typed correctly (pads/quilts/stoves/tents/filters/dry bags/towels). Feed weights good (208/368 had weight; most weightless were the archived parts/snowshoes).

**🆕 CNOC Outdoors — ✅ DONE 2026-07-01:** cnocoutdoors.com redirects to **minimalgear.com** (open Shopify, multi-brand family store). Removed the 3 old CNOC items (2 hand-made Amazon Vecto 2L/3L + 1 mislabeled "Atelier Longue Distance | CNOC"). Ingested vendor="Cnoc Outdoors" only via `--skip-brands "diorite gear,cowboy camping,sawyer products,minimal gear,tear-aid"` (⚠ `--skip-brands` matches VENDOR, and is bypassed if `--brand` is set — so DON'T pass `--brand`; rename after). 34 imported → renamed brand "Cnoc Outdoors"→"CNOC" → AI-type → archived 13 noise (bottle caps mistyped Hat/Headwear, valves/tethers/sliders/replacement parts, cups, food bag, merch hat). **CONSOLIDATED 2026-07-01** (`consolidate-cnoc.js`): the water containers were split into one item PER THREAD (28mm/42mm) → merged into **Thread × Volume** items (Vecto[1L/2L/3L], VectoX[2L/3L], Hydriam[350/500/750], HydriamX[350/500/750], Vesica[1L]); 42mm items archived, color collapsed. **11 active** (5 consolidated + VectoPlus + specials Triple Crown/PCTA/TKO/Artist + ThruBottle×2 + MUDPONS). ⚠ per-volume weights kept only where they VARY (HydriamX 29/31/34 g, Vesica 57 g); All 5 consolidated models now have per-thread/per-volume weights (user specs 2026-07-01): Hydriam, HydriamX, Vecto, VectoX, Vesica + capacity. CNOC weights DONE. **Same store still offers: Diorite Gear + Cowboy Camping (10) — potential future adds.**

**🆕 Diorite Gear — ✅ DONE 2026-07-02 (poles only, per user "trekking poles and nothing else"):** `create-diorite-poles.js`. Diorite's whole line = ONE pole product → imported **1 item: "Telescopic Carbon Fiber Trekking Poles"** (Grip axis Cork/EVA Foam, Trekking Poles, carbon/telescoping/lever-lock/carbide/padded strap attrs, 10 imgs, direct Minimal Gear offer). ⚠ **WEIGHT FIX:** feed lists the New pair 680 g for BOTH grips, but the per-pole singles give Cork 340 g (→680 g ✓) / EVA 283 g (→**566 g** pair) — feed EVA is a copy-paste error → Cork variant 680 g, EVA 566 g (default). **EXCLUDED (all of the other 38):** Blemished/Refurbished seconds, individual Left/Right singles, EVA Ultralight Staff, all parts/components (handles/bands/baskets/tips/lever assemblies/replacement sections), Diorite tent poles + carbon tent stakes, Long Haul Hat merch. Cowboy Camping (10) still not imported.

**🆕 LiteAF — ✅ DONE 2026-07-08 (`create-liteaf.js`, targeted create, NOT the generic ingest):** liteaf.com is WooCommerce w/ a WORKING Store API (`/wp-json/wc/store/v1/products`, 83 products), but the generic Woo importer collapses variable products to one flat item using the parent `weight` field — which turned out to be a rounded **shipping** weight, not real (e.g. 24 oz listed vs. 13.2 oz actual, confirmed against the description text). Real weights + torso/fabric tables live in each product's description ("WEIGHTS & MEASUREMENTS" section) — hand-read and hand-mapped to variation IDs/images/permalinks instead.
  - **18 "...Custom Backpack" listings dropped** — build-to-order against a chosen fabric+volume+torso combo, no fixed spec to catalog.
  - The ~30 remaining ready-to-ship "Curve" backpack SKUs are literally individual hand-sewn units currently in stock (fragmented one-off torso/color combos, not a clean size grid — e.g. one "Large" torso genuinely weighed less than a "Small/Small" of the same fabric, real-world hand-made variance). Consolidated to **item identity = Volume × Frame Type**, **variant axis = Fabric** (color + the noisy per-unit torso label both dropped), representative weight per fabric taken from the cleanest single-color listing at that combo: **Curve 20L Frameless** (Fabric[UltraWeave 200/200X/ECOPAK], 374–493g), **Curve 30L Frameless** (ECOPAK only, 538g), **Curve 40L Frameless** (UltraWeave 200X only, 672g), **Curve 40L Full Suspension** (Fabric[ECOPAK/UltraWeave 200X], 819–941g), **Curve 46L Full Suspension** (Fabric[ECOPAK/200X/200], 902–1009g — one ECOPAK source SKU (44119) turned out to only have generic marketing-banner photos on LiteAF's own site; swapped to a same-fabric sibling SKU (44131) with real photos, weight 902g).
  - **Multi-Day 35L / 20L** are real Woo variable products with clean per-size weight tables in the parent description — kept as designed: Multi-Day 35L = Torso Size[Small/Medium/Large] 473–476g; Multi-Day 20L = Daypack, flat 363g (color collapsed).
  - **14 small-gear items kept** (real LiteAF gear, not excluded-accessory noise): UltraTenX Ditty & Food Bags (Size, 13–38g), Printed ULTRA TX50 Ditty Bag (18g), Flat Bottom Bear Bag/Food Bag (Fabric[Dyneema/Ultra TX50], 37–53g — merged two separate listings that were the same bag in different fabric, per the "fabric = variant" rule), Round Bottom Pot Sacks (Size #1–#4, 4–5g; volumeLiters approximated from published cylinder dims since LiteAF doesn't state volume directly), Small Dyneema Zipper Pouches/Hiker Wallets (6g), UltraWeave Zipper Pouch Large (20g), Dyneema First Aid Zipper Pouch (Size, 7–12g), Ultralight Dyneema Stuff Sacks (Size, 8–17g; volumeLiters is a rough estimate from flat dims, brand doesn't publish volume), Dyneema Wide Mouth Tent Stake Stuff Sack (5g), Feather Weight Fanny Pack (Fabric[UltraWeave 200X/Dyneema/ECOPAK EPLX200], 65–74g), XL Eco-Friendly Fanny Pack (Fabric[200/400/EPLX200], 74–77g), Nylofume Pack Liner (26g), Hitchhiker Pack Towel (38g), UltraWeave Hip Belt Pocket/Pouch (Other, 27g — matches the Atom "Hipbelt Pocket" precedent for keeping belt-mounted pouches).
  - **Excluded as noise:** gift card, pin/sticker/beanie/trucker hat/canvas tote (merch), carabiner/sternum-strap-replacement/hip-belt-replacement/stick-em tabs (spare parts), phone pouch (EDC not backpacking), 2 duplicate fanny-pack listings (ULTRA200X Colors + a Miranda Goes Outside collab print — same physical product as the kept Feather Weight Fanny Pack, re-listed).
  - **21 active, 100% offer coverage (direct `merchantId:"direct-liteaf"`), 100% weighted.** Per-variant `deepLink` + `imageUrls` throughout (each fabric/size genuinely has its own liteaf.com product page). Category/subcategory set explicitly per itemType (not auto-derived on save — matches `CATEGORY_BY_ITEM_TYPE` in `inferItemType.js`). Backed up: `server/backups/dump-treklist_local-20260708T112131/`.
  - (⚠ NOTE for future Woo brands: Woo Store API sometimes works [LiteAF] and sometimes broken [Western Mountaineering] — always probe `/wp-json/wc/store/v1/products` before deciding no-feed. Individual `/products/<id>` fetches worked reliably here with a `Mozilla/5.0` UA + a small delay between requests — no Cloudflare trimming issue like GGG.)

**🆕 Bonfus — ✅ DONE 2026-07-08 (`create-bonfus.js`, targeted create):** bonfus.com is WooCommerce, Store API confirmed working (53 products), BUT the `weight` field is empty on every single product (not just wrong like LiteAF — literally never populated). Real specs live in a "Specifications"/"Weights" section rendered into each live product page's static HTML — format isn't consistent site-wide (some pages use a WPBakery accordion, others a plain `<h4>Weights</h4>` block) so pages were fetched live, stripped to text, and hand-read (reusable pattern: strip `<script>`/`<style>`, strip tags, collapse whitespace, then grep for "Weight"/"Specifications" headers).
  - **7 packs** (Volume = product identity, Fabric=color dropped, Pack Size/Hip Belt Size = variant where it's a real Woo attribute): Aerus 55L (Pack Size, **the only pack with genuine per-size weight**, 785–805g), Maxus 80L (Hip Belt Size, flat 1080g — no per-size weight published), Framus 58L/48L (Pack Size, flat 795g/760g), Saccus 48L (Pack Size, flat 480g), Fastus 23L + Iterus 38L (one-size, 360g/380g). "Altus - Custom Pack" dropped (build-to-order, no fixed spec — same call as LiteAF's Custom backpacks).
  - **4 pyramid shelters** typed **Backpacking Tent** (floorless mids, matches the Six Moon Designs Lunar Solo precedent — a floorless pyramid with its inner sold separately is still typed Tent, not Tarp Shelter): Middus 1P (307g), Middus 2P (457g), Solus 1P (509g), Duos 2P (Floor Fabric[Sil/PU / DCF], flat 688g — no per-fabric weight delta published). **DCF Square Flat Tarp** is a true flat tarp → **Tarp Shelter** (278g).
  - **4 "Middus Innernet" mesh/solid inner-tent add-ons** — real standalone products (own price/weight/page) but no matching itemType schema → **Other**: 1P Mesh (235g), 1P Solid (274g), 2P Mesh (335g), 2P Solid (374g).
  - **2 tent-stake products kept separate** (not merged into one Material-variant item — genuinely different lengths/profiles, 165mm shepherd-hook vs 150mm, not just a fabric swap): Titanium Shepherd Hook Stakes, Aluminum Tent Stakes. `weightGrams` = the **"Pack of 4" purchasable unit** (per-stake weight ×4 — 32g/34g), matching the "as-shipped" weight convention used elsewhere in this catalog.
  - **9 bags/pouches**: DCF Dry Bags + DCF Stuff Sacks (both Size[S/M/M-L/L], real per-size weights from each line's grouped parent page — individual size pages have zero photos/specs of their own, so images were sourced from the grouped parent and shared across all sizes), DCF Tent Stakes Sack, DCF Ditty Bag, DCF Food Bag (Size[Medium/Large]), Packing Cube (Size[Standard/Large]), Fanny Pack (Hip Pack), Shoulder Pouch + Stretch Mesh Shoulder Pouch + Hip Belt Pocket (no itemType match → Other, matches the LiteAF hip-belt-pocket precedent). **Skipped the "DCF 0.51 Stuff Sacks" line** (a lighter-fabric sibling of the kept Stuff Sacks) — its 4 size pages have **zero published weight AND zero photos anywhere on the site**, not enough real data to catalog honestly; revisit if Bonfus ever fleshes out that line.
  - **2 apparel items**: Alphus Pullover Hoodie + Alphus Full Zip Hoodie, Polartec Alpha Direct mid-layers → **Fleece Jacket** (`fleeceType: "Alpha Direct"`, matches the enum value added during the Sam Bob/FarPointe work), Size[S/M/L/XL] axis with real per-size weights, Color dropped (4 colorways, no spec difference), `category: "Unisex Clothing"` (matches the Sam Bob unisex-apparel convention — Bonfus doesn't gender-split these).
  - **Excluded as noise:** Gatekeeper Strap Set, Removable Webbing Belt, Removable Padded Hip Belt (hip-belt spare parts), UL Carbon Fiber Tent Poles (3 lengths) + pole extender (a-la-carte pole segments, no fixed product), Guyline 2.5mm UHMWPE (cordage), "Add-on" (a €0.99 checkout line item, not a real product).
  - **30 active, 100% offer coverage (direct `merchantId:"direct-bonfus"`), 100% weighted.** Per-variant `deepLink` + `imageUrls` throughout. Category/subcategory set explicitly per itemType. Backed up: `server/backups/dump-treklist_local-20260708T130727/`.
  - ⚠ **FOLLOW-UP (user, 2026-07-08): flat-weight-across-axis items must self-disclose in their description — reuse the Atom Packs wording where it's literally true, for consistency.** 5 items (Maxus 80L, Framus 58L, Framus 48L, Saccus 48L, Duos 2P) apply ONE Bonfus-published weight across a REAL variant axis (Pack Size / Hip Belt Size / Floor Fabric) because Bonfus itself never broke the number down by variant — a user picking "Large" would otherwise see a weight that looks verified for Large but isn't. Atom Packs already has this exact pattern (their catalog items carry no torso-size variant axis at all, and every description ends with `"\n\n* Listed weights are based on a Medium torso size; your configuration may vary."`). **Reused that exact sentence verbatim** on Framus 58L/48L, where it's literally true (Bonfus's own spec explicitly labels the number "(Medium)"). Where it ISN'T true, used a situation-specific line in the same terse `"\n\n* ...; your configuration may vary."` template instead of forcing inaccurate wording: Maxus 80L ("Listed weight is not broken down by hip belt size"), Duos 2P ("...by floor fabric"), Saccus 48L ("Bonfus does not publish a per-size weight breakdown" — the source never even states which size its number reflects). Applied via `CatalogItem.collection.updateOne` (never `.save()` on a projected doc); `create-bonfus.js` descriptions kept in sync for future re-creates. **Standing rule for every future brand import** (saved as memory `feedback-flat-weight-disclosure`): whenever a single published weight is applied flat across a real variant axis, disclose it in the description using this same template, reusing the Atom wording verbatim whenever the axis is genuinely a Medium-torso-based size (not just Bonfus).

**🆕 Opinel — ✅ DONE 2026-07-02 (via US distributor):** opinel.com itself has NO open feed (Apache/PrestaShop-ish), BUT **opinel-usa.com is an open Shopify store** (USD, 377 products, 96% with weights). ⚠ mostly out-of-scope: 96 kitchen knives (Large/Small/Table/flatware/Foodies), garden(11), wedding(14), kids(19), accessories/sheaths/storage(29), 53 PPLR_HIDDEN. **Relevant = folding pocket knives (~79** in product_types Carbon Steel(24)/Stainless Steel(21)/Premium(18)/Pocket Knife(16) — the classic No.06–No.13 in wood/steel variants). To import: ingest opinel-usa.com → keep the folding-knife types (drop kitchen/garden/etc.) → likely consolidate by knife number (No.08 across woods = variants or a few kept) → itemType **Pocket Knife**. **Replace** the 1 hand-made Amazon Opinel (N°06 Stainless, `B000UIIS38`). Direct opinel-usa.com links. **DONE (updated 2026-07-02):** **10 stainless folding knives** No.02–No.12 (`import-opinel.js` upsert), flat single items (carbon dropped per user), **real weights from Opinel's 'View All Sizes' chart** (5/7/9/16/34/40/45/57/71/111 g), lock + blade material/length in description/tags, direct opinel-usa.com offers. Pocket Knife (empty attr schema → specs live in description/tags).

**🆕 Altra — ✅ DONE 2026-07-08 (`create-altra.js`, targeted create):** altrarunning.com is Shopify (open `en-us/products.json`, 408 products). `product_type` empty on every product and variant `grams` is 0 everywhere (confirmed, matches the weight-trust rule warning) — real specs (Weight/Drop/Stack Height/Cushion/Best For/Upper/Midsole/Outsole) scraped from a consistent "All Details" accordion on each live product page (same regex works across every product — unlike Bonfus, Altra's page template is uniform).
  - **Scope narrowed across 3 rounds with the user** (started from 408 products): 315 are gendered shoe listings → 149 fully out of stock (0 purchasable variants, old discontinued generations) dropped, leaving 166 in-stock → kept only the **latest generation per model line per gender** + that generation's real construction sub-variants (GTX/BOA/Hiker/Mid/ALL-WTHR are different shoes, not color reskins) + dropped one-off collabs (SOAR, "and wander", "X Pleasures") → 77 → the schema only has itemType **"Trail Running Shoes"** (no "Road Running Shoes"), and Altra's own `SHOP_*_SHOES_ROAD` tags cleanly separate everyday/road models (Escalante, Torin, Paradigm, Solstice XT, Vanish, Provision, Rivera, VIA Olympus, FWD VIA, Motiv FLX, Experience Flow/Form) from trail/hiking models — kept only the trail/hiking-tagged lines. **Final: 40 items** across 10 core lines: Lone Peak 9+ (+GTX, +ALL-WTHR Low/Mid 2, +Hiker 3), Timp 6 (+GTX, +Mid GTX, +Hiker, +Hiker GTX), Olympus 275 (+HiLo), Mont Blanc (+Carbon, +Speed), Superior 7, King MT 2, Experience Wild 3+, LP Alpine, Voyager.
  - **No variant axes modeled** (Size, or Width on Lone Peak 9+ which has a real Regular/Wide option) — weight is ONE number per shoe regardless of size site-wide (standard industry reference-size convention), matching the pre-existing "Trail Running Shoes" precedent in this catalog (La Sportiva Wildcat 2.0 GTX also has no variantAxes).
  - ⚠️ **Weight-disclosure caveat added (user, 2026-07-08):** the scraped weight is per SINGLE shoe at a manufacturer sample size (not the pair, not the user's size — Altra doesn't publish which size), so each of the 36 weighted items got a trailing `"\n\n* Listed weight is per single shoe at a manufacturer sample size; actual weight varies by size."` in its description — same terse template as the Bonfus/Atom flat-weight lines, and directly guarding against the exact misread that produced the archived legacy pair-weight bug. Applied via `updateOne` + kept in sync in `create-altra.js`. (Different trigger from Bonfus's: there it's a flat weight across a real variant axis; here there's no axis but the single published number still isn't the pair or the user's size. Both fall under the same standing rule — see memory `feedback-flat-weight-disclosure`, now broadened.)
  - **4 items have no published weight anywhere on their page** (Lone Peak ALL-WTHR Low 2 both genders, Mont Blanc plain both genders) — imported with `weightGrams` left null rather than fabricated.
  - **Images:** the feed only tags the HERO shot of each colorway with `variant_ids`; the 5 ALT gallery shots share the same filename prefix (e.g. `AL0A85RG88D-HERO`/`-ALT1..5`) but aren't variant-linked — matched by prefix to recover the full photo gallery for one representative color per shoe.
  - ⚠️ **Found + fixed a pre-existing data bug while verifying:** 4 legacy manual entries from Feb 2026 ("Lone Peak 9 - Men's/Women's", "Timp 6 - Men's/Women's") had weights almost exactly **double** the real per-shoe weight (Timp 6 Men's: 649g legacy vs 325g from Altra's own spec page; ratio 1.997×; Women's ratio 2.000×) — looks like a pair-weight-recorded-as-single-weight bug. Archived all 4 per user's call (2026-07-08) rather than fix-in-place, since two were exact-model duplicates of today's import and two were an older generation already out of scope. **Worth a broader audit**: if this pair-weight bug exists on other pre-2026-07 manual footwear entries, they'd be silently overweighted by 2×.
  - **Apparel NOT imported** (hoodies/socks) — Altra's core identity here is footwear. **Trail Gaiter added 2026-07-08** (`create-altra-gaiter.js`, +1 → Altra 41 active): a strapless low trail gaiter, typed **Hat/Headwear** / Unisex Clothing / Headwear (matches the OR trail-gaiter convention — no dedicated Gaiter itemType; bare "gaiter" falls back to Hat/Headwear), Size[S/L] variant (color dropped), no attributes (OR precedent), no weight (Altra publishes none), single item-level offer (size is an on-page selector).
  - **40 active, 100% offer coverage (direct `merchantId:"direct-altra"`).** Backed up: `server/backups/dump-treklist_local-20260708T143015/` (post-weight-disclaimer state).

**🆕 Hoka — researched 2026-07-02, ❌ NO open feed:** hoka.com is **Salesforce Commerce Cloud** (Deckers), heavily bot-protected — `products.json` 410 Gone, homepage 406 to scripted requests. Same blocker as Osprey/Smartwool/Mountain Hardwear → needs **AvantLink/Impact affiliate feed** or SFCC OCAPI (or manual copy-paste). Added to no-feed table below.

**📌 IMPORT PRECEDENCE + ANTI-JUNK RULE (user, 2026-07-02):** applies to GGG and ALL future imports. (1) **Brands/items we've already imported TAKE PRECEDENCE — do NOT replace or re-import them** (existing curated item always wins; use the `dedupe-ggg.js` existing-wins pattern; never re-run the feed importer over a brand we've hand-curated → it can clobber hand weights/variants). (2) **Be selective — exclude junk.** Only bring in backpacking/hiking-relevant items; drop climbing-hardware / ski / spare-parts / merch / apparel-noise. Prefer filtering by feed `product_type` to a keep-list, then curate.

**🟢 Pa'lante Packs — ✅ DONE DIRECT 2026-07-02 (Shopify):** palantepacks.com is Shopify. Imported **9 packs** via `ingest-shopify-catalog.js --brand "Pa'lante" --collapse-color --exclude "<apparel/merch/bundles/seconds>"` → typed (8 Backpack + fanny=Hip Pack). Real per-variant weights (Fabric×Torso, vary sensibly). Skipped apparel (t-shirts/shorts/skirt/fleece hoody/mock neck), merch (gift card/pipe/rolling tray/cork ball), bundles (w/ pad+sleeve, joey straps), seconds (sample/b-grade), archives/test products, sidebags, water bottle, tenkara rod. ⚠ names are lowercase (Pa'lante's real branding). ⚠ V2's variant axis came through as "size × color/material" (fabric+color combined in the Shopify option) — slightly messy but weights correct; refine later if wanted.

**🟢 FarPointe Outdoor Gear — ✅ DONE DIRECT 2026-07-02 (Squarespace scrape):** farpointeog.com is **Squarespace** (no products.json; feed via `?format=json`, product pages scraped). **Alpha Direct / TecnoWool active-insulation APPAREL** brand (not packs). Imported **13** (`import-farpointe.js`): Alpha Cruiser/Crewneck/Printed/Super/Duo/Yakona + TecnoWool Cruiser/Crewneck = Fleece Jacket; Sun Cruiser = Hiking Shirt; Alpha Camp Pants = Base Layer Bottom; Camp Socks = Hiking Socks; Cap = Hat/Headwear; Wool Gaiter = Neck Gaiter. ⚠️ **WEIGHT (per user): FarPointe publishes weight for SIZE MEDIUM only, varying by fabric GSM (60/90/120gsm) → `weightGrams` = representative Medium (90gsm), and the FULL "Weight (based on size Medium): …" breakdown is written into each DESCRIPTION.** Camp Socks + Yakona don't publish weights → blank + "Weight not published" noted. Descriptions + images scraped from product pages (JSON-LD desc + squarespace-cdn imgs). Direct FarPointe offers. **Squarespace scrape pattern reusable** (`?format=json` list + HTML JSON-LD per product).

**🟢 Exped — ✅ DONE DIRECT 2026-07-02 (full catalog scrape, `import-exped.js`):** exped.com is a **custom Nuxt/Drupal SSR** site (no feed; sitemap `/en/products/<category>/<slug>` = 252 products). Scraped **235** (excluded ~16 straps/ropes/cords/parts per user: strap/carabiner/tool-blade/repair-kit/valve-adapter/coupler-kit/chair-kit/tent-cord/slit-line/helmet-holder/flash-pack-pocket/hip-pads). itemType from URL category: 53 Backpack, 46 Inflatable + 5 Foam Sleeping Pad, 23 Sleeping Bag + 2 Quilt + 1 Liner, 23 Backpacking Tent + 4 Tarp Shelter + 8 Ground Sheet + 5 Tent Stakes, 22 Dry Bag/Stuff Sack, 9 Pillow, 5 Rain Poncho, 2 Hip Pack, 1 Hiking Socks, 26 Other (pumps/bivybags/hammocks/mat-covers → Sleep-Pad-Accessories/Shelter/Footwear). **233/235 have real weights** parsed from the rendered spec table (packs="Min. Weight", others="Weight", size token like "S/M:"/"1 l:"/"DUO M:" handled, kg→g; 2 genuinely unpublished left blank). Images = rokka.io CDN (deduped by filename, 933px). Descriptions from JSON-LD. Direct Exped offers. ✅ **ATTRIBUTES FILLED 2026-07-02** (`exped-attributes.js`, 152 items, 0 empty): bags = temp°C/°F + insulation (Down: Ultra/Comfort/Dura; Synthetic: Terra/Deepsleep/MegaSleep/DreamWalker/LuxeWool — by MODEL name, since "down" appears in prose) + shape/gender; pads = rValue (from "3R"/SIM number/desc) + shape + inflationMethod; packs = volumeLiters + gender (**sub-20L packs retyped Backpack→Daypack** — Backpack requires vol≥20; <10L drops volume); tents = capacity (Roman/digit) + seasonRating (Extreme→4-Season). ✅ **CONSOLIDATED into variants 2026-07-02** (`exped-consolidate.js`, "like Zpacks/HMG", 22 families, **235→186 active**, 49 archived, per-variant weights+attrs, ONE offer each, 0 orphan offers): **bags→Temperature** (Ultra/Comfort/Deepsleep/Dura/Terra — ⚠ Ultra had metric "0C 30F" + degree "0°" DUP slugs → deduped by °C, kept metric, archived 3 degree dups); **tents→Capacity(×Fabric)** (Cassira/Lyra/Outer Space/Vega single-axis; Orion/Venus = Capacity×[UL/Extreme]; Ceres = Capacity×[Extreme/XP Extreme]); **pads→R-Value, shape split** (Ultra/Ultra Mummy/Ultra Duo Mat; Dura; SIM/SIM Ultra/SIM Comfort/SIM Comfort Duo; Versa; Flex). ✅ **PACKS + CONFIG-MATS CONSOLIDATED 2026-07-03** (2nd pass, same script): **20 pack families → Volume axis** (Mountain Pro/Couloir/Verglas/Skyline/Impulse/Icefall/Serac/Torrent/Lightning/Cloudburst/Metro/Summit Hike/Summit Lite/Black Ice/Ridgeline/Typhoon/Cruiser/Whiteout; women's = separate "… Wmns" parents to avoid a ragged Gender axis; WhiteOut/Whiteout casing merged; parent = largest volume so the Backpack `volumeLiters≥20` base is valid; families span Backpack+Daypack via `type:PACK`). **3 config-mat families → Configuration axis** (MegaMat = Standard/Auto/Duo/Max/Max Duo/Ultra; LuxeMat + DeepSleep Mat = Standard/Auto/Duo; parent = "Standard"). Exped **185→147 active**; 45 variant items, 129 variants, all with per-variant weights/images/links, 0 orphans. ⚠️ **BUG FIXED mid-run:** the archive step assumed parent=variants[0]; for volume/config kinds the parent isn't index 0, so it archived the parents — fixed to exclude `parentRow` by _id, restored from backup, re-ran clean. **Still standalone singletons:** Work & Rescue 50, Splash 15, Typhoon Kids 15 (packs); Multimat, Doublemat, Versaluxe, SIM Lite, Air Seat/Sit Pad/Flex Sit Pad, Dura Duo (mats); MegaSleep, DreamWalker, LuxeWool ×2, Ultra Quilt, Ultra XP (bags). ⚠ multi-size packs/bags: weightGrams = smallest-size published weight. Reusable **Nuxt/Drupal SSR scrape pattern** (strip `<style>`/`<script>` first). _(Old GGG-6-mats back-door obsolete — did full direct.)_
  - ✅ **PER-VARIANT IMAGES 2026-07-02** (fixed a real bug: consolidated variants that look different — 1P vs 3P tent, 1R vs 10R pad — were all showing ONE image): added `imageUrls` to the variant sub-schema (`catalogItem.js`), consolidation now captures each member's images into `variant.imageUrls` (re-ran from the pre-consol backup so all 22 Exped families got per-variant photos), client shows the selected variant's images (`CatalogItemPreviewModal.jsx` + `GlobalItemEditModal.jsx`, fall back to item images), and the admin `normalizeVariantSet` preserves `variant.imageUrls`. ✅ **PER-VARIANT BUY-LINKS 2026-07-03** (user: "route to the one they selected, not a placeholder"): added `deepLink` to the variant sub-schema; the click resolver (`affiliates.js`) + add-from-catalog (`globalItems.js`) + client buy buttons all route to `variants[variantKey].deepLink`. Exped's 22 families each route to their own exped.com page. **Backfill for PROD: `backfill-variant-links.js`** (0 owned locally). ⚠️ Amazon per-variant ASIN/region routing still TODO — see [[project-per-variant-offer-routing]]. _(Also cleaned 3 import artifacts: reactivated Bivybag VentAir/PU + Lite Ventair/PU with decoded names, dropped dup "Schnozzel" → Exped 185 active.)_ ⚠️ **Other pre-existing consolidated brands (Zpacks/HMG/Nemo/Osprey/etc.) still share item-level images** across variants — mostly OK there (temp/size variants of the same-looking product), but if any need per-variant photos they'd have to be re-sourced (feed variants didn't carry distinct images).

**🟢 Black Diamond — ✅ DONE 2026-07-08 (`create-blackdiamond.js`, targeted create, config-driven):** blackdiamondequipment.com Shopify, 1071 products, mostly climbing + ski. **Feed weights are REAL and often per-size** (unlike LiteAF/Bonfus/Altra where feed weight was junk) — used directly, only fixing 2 bad values (Storm+ Headlamp 8200g→145g data error; gaiters had a flat placeholder 200g across every model+size → nulled). **120 active** across these itemTypes: Headlamp 15, Camp Lantern 5, Trekking Poles 18, Backpack 8 + Daypack 2 + Hip Pack 1, Backpacking Tent 13, Hat/Headwear 4 (gaiters), **Other 50** (the climbing/mountaineering gear).
  - **Category decision made with the user ("back and forth on categories"):** core backpacking IN (Headlamps, Lanterns, all Poles, Hiking Packs, Tents, Gaiters) + user explicitly wanted **Helmets + Harnesses** + user-selected adjacent buckets **Mountaineering (Crampons + Ice Axes)** and **Small accessories (microspikes/traction, headlamp batteries)**. User DECLINED running/hydration packs and approach shoes. ⚠ **No via ferrata leashes exist in BD's catalog** (BD doesn't make them — that's Petzl/Edelrid/Climbing Technology).
  - **Helmets/harnesses/crampons/ice-axes/microspikes/batteries → itemType "Other"** (no matching schema type in a backpacking app; gender/model kept in the name). Backpacking-core types got their name-parseable headline attr: headlamp `maxLumens` (from name, e.g. Storm 500→500, Distance 1500→1500), pack `volumeLiters`+`gender` (Pursuit 30, Trail Vista 28; women's = Womens else Unisex; <20L→Daypack, waist packs→Hip Pack), tent `capacity` (from "X Person"), pole `material`(carbon/alu)+`soldAs:Pair`. Size axis modeled where the feed had it (helmets S/M·M/L, poles, ice axes, harnesses, traction — all with real per-size weights; color always collapsed).
  - **Excluded:** all ski gear, pure climbing hardware (carabiners/quickdraws/protection/ropes/belay/chalk/big wall/crash pads/climbing packs/ice screws+tools), all apparel, rock climbing shoes, running packs + approach shoes (declined), spare parts, duffels, gift cards. Per-item junk dropped via NAME_EXCLUDE: pole FKT straps, a headlamp Bundle SKU, Heavy Metal Gear Sling (not a harness), Wiz Kid (kids), Big Wall Hooped Bivy. **⚠ Bivy = 0 imported:** user wanted the UL Spotlight Bivy, but it's out of stock and only the (dropped) Big Wall one was live — revisit if Spotlight restocks.
  - ⚠️ **Legacy reconciliation (same as Altra):** 10 old thin manual BD entries existed (Jan–Mar 2026). Archived 4 that this richer feed import supersedes (Alpine Carbon Cork Hiking Poles, Storm 450, Distance Carbon Z Poles, Distance Carbon FLZ Poles — via `SUPERSEDE` list, archive-legacy-keep-fresh) so the feed versions (real weights, Size variants, full galleries) win. Kept 4 legit non-duplicated actives (Distance Aluminum FLZ/Z Poles = discontinued, not in feed; Fineline Stretch Shell + Men's Crag Gloves = apparel out of scope). 2 pre-existing inactive dupes (Men's Solution Harness, Vision Helmet) re-created fresh+active from the feed.
  - **PHASE-2 follow-up (deeper attrs, not done):** headlamp burn time / beam distance / IP rating / battery type, pole lengths+lock+fold, tent season rating (4 named tents Eldorado/Bombshelter/Ahwahnee/Fitzroy also lack a parseable capacity), lantern battery — all live in BD's spec tables and would need a per-page scrape (BD pages are structured, so scriptable). Same "structure now, deep attrs later" as OR/Nemo.
  - **120 hard-good active, 100% offer coverage (direct `merchantId:"direct-blackdiamond"`), weights on all but the 4 gaiters.** Backed up: `server/backups/dump-treklist_local-20260708T174017/`.
  - **🧥 APPAREL PASS ✅ 2026-07-08** (`create-blackdiamond-apparel.js`, user-requested, +190 active → BD 308 total): imported the requested clothing categories, gendered. **BD's apparel feed weights are REAL and per-size** (parkas 764→1014g by size) → captured per Size variant, no reference-size disclaimer needed (BD publishes them). Per-ITEM name-based typing (feed product_type ≠ gear taxonomy): Shells→Rain Jacket except softshells (Alpine Start/Distance Wind Shell)→**Softshell Jacket** (new type, user's call); Insulation→Insulated Jacket (38); Fleece→Fleece Jacket except Coefficient LT line→Base Layer Top (6); **Hoodies split by kind** (user's call): Alpenglow sun hoodies→Hiking Shirt (5), rest→Fleece Jacket; Pants→Hiking Pants except rain→Rain Pants + Coefficient LT→Base Layer Bottom (3); Shorts→Hiking Shorts (26); all non-ski Gloves (Everyday/Mountaineering/Climbing/Running)→Gloves (Insulated) (31); Beanies→Hat/Headwear except "Coefficient LT Gaiter"→Neck Gaiter. **Excluded per user's list:** Tees, Shirts, HEX line, ski gloves, socks; the 3 named merch hoodies (Engineered Diamond / Mini Stacked Pullover / Mini Stacked Full Zip); and "Beta Belt" (a webbing belt mis-filed under Beanies). Superseded 2 thin legacy apparel entries (Fineline Stretch Shell, Men's Crag Half-Finger Gloves). Attributes shallow (gender from name); deep apparel attrs (membrane/fill/fabric weight) = possible phase-2, matching OR-apparel precedent. **190 active, 100% offer + image coverage.** Backed up: `server/backups/dump-treklist_local-20260708T181036/`.

**🆕 Feathered Friends — ✅ DONE 2026-07-09 (`create-featheredfriends.js`, direct Shopify, vendor-filtered):** featheredfriends.com is Shopify with an open feed, BUT FF is a **multi-brand RETAILER** (Arc'teryx 195, Patagonia 45, OR 50, Hilleberg 34, Rab 25, La Sportiva 34, MSR, Osprey, HMG, Sea-to-Summit… in the feed) as well as a maker — imported **ONLY `vendor="Feathered Friends"`** (their own premium down line = 74 products), never the resold brands (they'd duplicate our direct imports — the GGG/MEC "mud" rule). ⚠ **Feed variant `grams` are ROUND SHIPPING weights** (Swallow UL = 1814g/4lb flat across every temp+length; useless). **Real weights + temp ratings + fill weights are in a spec table embedded in each product's `body_html`** (no page scrape needed) — parsed here (`parseTable`, validated 41/41 non-kids bags; handles imperial-only cells, `68"` inch labels, "Not Rated", women's "Med" abbrev).
  - **Sleeping bags/quilts (64 items from 41 products): TEMP-SPLIT per the locked REI standard** — FF sells many as one Shopify product with a Temperature option (Swallow UL 20/30, Condor YF 10/20/30, Penguin YF 10/20/30/40); each temp → its own CatalogItem (Swallow UL 20 / Swallow UL 30), **Length** the only variant axis (Regular/Long, Small/Medium women's, or 68"/74"), color dropped. Per-temp per-length real weights from "Average Weight"; `fillWeightG` from "Fill Weight"; `fillPower` from tags (900+/950+); `tempRatingF`+`tempRatingC`; `insulationType: Down`; `shape: Mummy` (omitted on quilts + the Spoonbill 2-person). **Flicker "…Quilt" → Quilt itemType (12)**; rest → Sleeping Bag (52). Women's = separate products.
  - **Down garments (10) → Insulated Jacket** (gendered, Size axis, color dropped): Eos/Helios/Volant jackets+vests, Khumbu/Rock&Ice expedition parkas. Weight = published **size-Medium reference** → item weight + a `"\n\n* Listed weight is for size Medium; actual weight varies by size."` disclaimer (the reference-size rule).
  - **Excluded:** kids' bags (2); home bedding (Down Comforter/Throw/Upcycled Blanket/Bedding Accessories + the Organic Down Pillow — FF's bedding line, not backpacking); down pants + Expedition Down Suits (niche, no good itemType); Closeout / Previous-Model dupes.
  - **74 active, 100% offer + weight + image coverage** (direct `merchantId:"direct-featheredfriends"`). Backed up: `server/backups/dump-treklist_local-20260709T071554/`.
  - **RESOLD APPAREL PULLED ✅ 2026-07-09** (`create-ff-resold-apparel.js`, user: "super valuable even if the retailer is different; note it and swap the retailer when we have access"): imported **Arc'teryx (126) + Patagonia (31)** apparel from FF's feed (vendor-filtered, in-stock, not `bis-hidden`; 169 in scope, 14 same-name/season dupes collapsed → 157). **Typed by FF's garment-type TAGS** (Top/Bottom/Short/Handwear + Hardshell/Softshell/Fleece/Insulation/Down/Baselayer → itemType; 100% tag coverage) rather than Arc/Pat's cryptic model names. brand = actual brand; **offer = Feathered Friends resold (see the RESOLD-OFFER note at the top to re-point later)**. Weightless. Used the new Softshell Jacket type heavily (Squamish/Gamma). FF's Arc/Pat = apparel only (Arc 195 / Pat 45 all "Clothing"); did NOT import the resold OR/MSR/Osprey/HMG/Sea-to-Summit (we have those direct).
  - **🔑 HILLEBERG — ✅ DONE 2026-07-09 (`create-hilleberg.js`, direct Nuxt-SSR):** did the full direct scrape, not the FF 6-tent back-door. **hilleberg.com/eng is Nuxt SSR and every page's `__NUXT_DATA__` is PLAIN JSON containing the ENTIRE structured tent catalog** (379 spec records) — so ONE page fetch (any family page e.g. `/eng/products/tent/Nallo`) yields all tents with full specs. ⚠ __NUXT_DATA__ is a deduplicated flat array — object field values are INDICES into the array, resolve with `arr[idx]`. ⚠ enumeration gotcha: sitemap sparse (26 URLs), category + numbered-size pages return HTTP 500; only single-name + family pages (`/eng/products/tent/<ProductGroup>`) return 200, but any one carries the whole catalog. **43 tents imported** (48 distinct published; excluded the 4 Blue Label group shelters Altai/Atlas/Stalon — 6-14 person, 11-26kg, over the schema's 4-person cap & not backpacking; also archived Akto Anniversary post-import per user — a limited-edition Akto dup, 1P/1300g, whose CDN image 404s; now excluded in the script too → 43 active). Each record has `occupants`→capacity, `minMetric`→weight (g), `construction`→season (All Season→4-Season, Three Season→3-Season), `type`→tentType (Tunnel→Non-Freestanding, Dome-Free Standing→Freestanding, Dome-Self Supporting→Semi-Freestanding), `fabric`→flyMaterial (Kerlon 1000/1200/1800), + floor area, height, doors, vestibules, prices. Colors collapsed (dedupe by name); GT variants kept separate (distinct models); capacity already per-size in the data (no split needed). Direct Hilleberg offers, deepLink to family page. **100% offer + weight + image coverage.** ⚠ images = CDN 300px transparent PNG renders (the -1200- upscale mostly doesn't exist; usable, upgradeable from the family-page galleries later). Backed up: `server/backups/dump-treklist_local-20260709T080638/`. (Same direct-scrape likely works for Rab/Samaya/Blue Ice — FF resells them too; check each.)

**🟢 Garage Grown Gear (GGG) — BACK IN (user, 2026-07-02), IMPORTABLE (Shopify), SELECTIVE:** open feed `garagegrowngear.com/products.json` (~185 products / 78 vendors). Re-add **by vendor only** — the no-feed brands we're blocked on + unique cottage brands — SKIP vendors we import direct (Gossamer/HMG/Zpacks/Sea to Summit/LiteAF/Cowboy Camping). Unlocks **Cumulus, Tarptent, Pa'lante, Katabatic, FarPointe** + TOAKS/Darn Tough/Klymit/Exped/Outdoor Vitals/etc. Full plan under "Where things stand → GGG BACK IN THE GREEN". (Reverses the earlier full-mud removal — this is the surgical re-add.)
  - **▶ STARTED 2026-07-02** (`import-ggg-cumulus-tarptent.js`): **Cumulus (4)** — Panyam 600 Sleeping Bag (970g), Spotter 600 (989g), Neo Quilt 150 (269g), The Quilt 150 (300g) — all Down w/ FP + fill + comfort/limit temps; **Tarptent — added ProTrek then the 4 more the trimmed feed hid** (`import-ggg-tarptent-rest.js`): Aeon Li (478g, DCF), Double Rainbow (1038g, 2P), StratoSpire 2 (1244g, 2P dbl-wall), Notch (811g, 1P dbl-wall) → **6 Tarptent active** (incl. pre-existing **Rainbow**, left untouched per precedence). ⚠ **feed grams were reseller shipping/packed weights (Panyam 1970/Spotter 1850; Aeon Li 794 vs real 478) — used the REAL weights + temps read from GGG descriptions instead.** GGG direct offers.
  - ⚠️⚠️ **GGG FEED TRIM GOTCHA (bit us here):** node `fetch` on `products.json` gets a **Cloudflare-TRIMMED** list (~185, so Tarptent showed 1 not 5, Cumulus 4 not 10). **Use the per-vendor collection via curl: `curl -A <UA> "https://www.garagegrowngear.com/collections/<vendor-slug>/products.json?limit=250"`** for the true full vendor list. (Individual `/products/<handle>.json` are NOT trimmed — fine via node.)
  - ✅ **Six Moon Designs (7 shelters) + Alpenglow (1 pump) — 2026-07-02** (`import-ggg-smd-alpenglow.js`): SMD tents only (Lunar Solo, Haven 2P, Lunar Duo Explorer/Outfitter, Skyscape Trekker/Scout = Backpacking Tent; Gatewood Cape = Tarp Shelter) — skipped SMD umbrellas/footprints/pack-liner/pods; existing SMD "Ultralight Hiking Umbrella" untouched. Alpenglow **Alpenblow Classic Micro Inflator** (USB-C pad pump, 8.5g→9g, itemType Other → Sleep System/Sleeping Pad Accessories; skipped the fuel valve + nozzle pack). Real weights from descriptions (feed grams inflated: Lunar Solo 907 vs 737; pump 21 vs 8.5). ⚠️ **ALSO:** individual `/products/<handle>.json` node-fetches intermittently return EMPTY (Cloudflare) when hit rapidly → the script now fetches each via **curl w/ 4× retry** (`fetchProduct()`). Use that pattern for GGG product pulls.
  - ✅ **Western Mountaineering (5) — 2026-07-02** (`import-ggg-wm.js`): VersaLite/UltraLite/FlyLite bags + NanoLite quilt + Tioga silk liner (booties skipped). Real total weights from descriptions.
  - ✅ **Cumulus COMPLETE — 10 active** (`import-ggg-cumulus-rest.js` added the 6 the trim hid): Aerial 180/250/330 (300/383/482g, 900FP quilt-bag hybrids, hoodless), Magic 100 (240g summer), Vencer 100/200 (180/375g half-bags). Real TOTAL weights + comfort temps from descriptions (feed grams were DOWN-FILL/bogus; fill weight = model number, e.g. Aerial 180=184g fill). All 900FP down, GGG direct offers. **Remaining GGG targets:** Katabatic (via GGG — already have direct → SKIP); Pa'lante + FarPointe = **done DIRECT instead** (user prefers direct over GGG, see below); + unique cottage brands still open (TOAKS/Darn Tough/Klymit/Exped/Outdoor Vitals).

**⚠️ No open feed → affiliate network (AvantLink/Impact — user lacks) or per-page scrape:**
- **Sawyer** (sawyer.com) — researched 2026-07-01. **Cloudflare + not Shopify** (products.json 404) → no open feed. Currently **3 resold Sawyer filters active** (Squeeze/Mini/Micro Squeeze via HMG/GGG) + 1 HMG-resold dup. Stays resold unless affiliate/manual. Water filters (Water Filter type).
| Brand | Domain | Platform | Path |
|---|---|---|---|
| **Enlightened Equipment** | enlightenedequipment.com | BigCommerce + Cloudflare | Own site no open feed — **✅ DONE via Farlite back-door 2026-07-03** (`import-farlite-ee.js`): 7 imported — Revelation APEX 20/30/40F (synthetic quilts), Revelation 20F + Enigma 20/30F (down quilts, 850FP), Cloud 9 UL Pillow. Skipped Torrid Jacket, Rain Wrap, Pad Straps (apparel/accessory). **✅ consolidated 2026-07-03** (`ee-consolidate.js`): Revelation APEX (40/30/20°F) + Enigma (30/20°F) → Temperature axis, per-variant links/images; Revelation 20F + Cloud 9 pillow singletons. **EE = 4 active.** |

**🟢 Farlite (farlite.fi) — researched 2026-07-03, IMPORTABLE (WooCommerce Store API works), MULTI-BRAND back-door:** Finnish UL retailer, `/wp-json/wc/store/v1/products` returns 600 products (⚠ **each listed TWICE — EN + FI category**, `-2` slug = EN; dedupe + prefer `/en/` permalink). Carries **Cumulus (12 unique)** + **Enlightened Equipment (10 unique)** + others — the back-door for EE (no direct feed) and extra Cumulus. Weights = real totals in the Woo `weight` field; comfort/limit temps + FP in the (English) descriptions. Direct Farlite offers (`merchantId:"direct-farlite"`). **DONE:** 6 Cumulus sleep gaps (`import-farlite-cumulus.js`). **NEXT:** EE quilts/bags via same pattern.

**🟢 Big Agnes (bigagnes.com) — ✅ DONE 2026-07-03, QA'd 2026-07-04:** open `products.json`, 286 products, TENT-heavy. Imported core backpacking (tents/bags/pads/backpacks/footprints), skipped furniture/luggage/apparel/most accessories. `type-big-agnes.js` (name-based itemType — BA model names lack the category word) → `collapse-big-agnes-colors.js` (folded colorway/edition dupes) → `big-agnes-consolidate.js` (tents→Capacity, Sweetwater→Volume) → then **de-consolidated 2026-07-03** (`deconsolidate-big-agnes.js`) onto the locked REI standard: each primary capacity/temp/volume is its own product again (Copper Spur UL2, Anthracite 20°, Sweetwater UL 60L…), Size is the only variant axis, deepLinks point at the real per-product Big Agnes feed productId. **116 active** (38 Backpacking Tent, 36 Ground Sheet, 21 Sleeping Bag, 7 Inflatable Sleeping Pad, 4 Backpack, 3 Sleeping Bag Liner, 2 Foam Sleeping Pad, 2 Pillow, 1 Quilt, 2 Other), 100% offer coverage. Included in the 2026-07-04 accuracy QA pass (`qa-audit.js` — see `server/reports/catalog-qa-2026-07-04.md`).
| **Kahtoola** | kahtoola.com | BigCommerce + Cloudflare | Microspikes/traction. API auth or scrape/affiliate. |
| **Western Mountaineering** | westernmountaineering.com | WooCommerce | Store API **broken** — but **✅ done via GGG back-door 2026-07-02** (`import-ggg-wm.js`): 5 imported (VersaLite 10°F/UltraLite 20°F/FlyLite 36°F bags, NanoLite 38°F quilt, Tioga silk liner). GGG also has Flash Down Booties (skipped, not requested). |
| **Granite Gear** | granitegear.com | old custom CMS | `/products.json` → homepage; no feed. Packs → affiliate (AvantLink/Impact) or scrape. |
| **Mountain Hardwear** | mountainhardwear.com | Demandware/SFCC + Cloudflare + PerimeterX (Columbia) | ❌ **CONFIRMED no open feed 2026-07-09.** products.json/Woo 404. OCAPI Shop API IS reachable (site id `MountainHardwear_US`, `/s/MountainHardwear_US/dw/shop/v21_3/product_search`) but needs a `client_id` credential (classic SFRA, none exposed). PLP pages PerimeterX-gated. **Affiliate network = CJ (Commission Junction)** — homepage carries CJ + Rakuten tags, NOT AvantLink/Impact. So MHW's realistic unlock = a **CJ Affiliate** account approved for the MHW program (a network we don't track yet), or manual copy-paste (Osprey pattern). |
| **Deuter** | deuter.com | enterprise (nginx, custom) | No open feed. Packs → affiliate or manual copy-paste. |
| **Hoka** | hoka.com | Salesforce Commerce Cloud (Deckers) + bot-block | products.json 410, homepage 406. Trail/road shoes → affiliate (AvantLink/Impact) or manual. |
| **Petzl** | petzl.com | Salesforce Commerce Cloud (B2C login wall) | products.json → redirects to `/B2C_Web_Login`. Headlamps/traction/harnesses → affiliate or manual. (Headlamps are the relevant bit; Black Diamond covers that niche with an open feed.) |

**Recommended order (Shopify, quick wins first):** Vargo → Katabatic → Zenbivy → Gossamer Gear → Sam Bob, then the big filters (Sea to Summit, Outdoor Research). The 6 no-feed brands wait on AvantLink/Impact access or a scrape decision (same blocker as Osprey/Nemo affiliate).

### HMG (Hyperlite Mountain Gear) DONE — ✅ 2026-06-29
- Shopify feed (118 products), structured like Zpacks. Imported 110 (apparel/junk excluded) → **73 active**. 14 pre-existing manual single-size items (Windrider 40/55/70 etc.) **archived** (superseded by feed's variant versions). 23 packs with Volume×Torso variants + per-variant weights + attrs (DCF vs UltraWeave best-effort); 7 tents (UltaMid/Mid/CrossPeak/Unbound) + Flat Tarp + Ground Cloth; quilts/stuff-sacks/hip-packs/stakes attributed. Archived 37 accessories/bundles/poles. Scripts: `curate-hmg-{cleanup,attrs-packs,attrs-shelters,attrs-rest}.js`. ⚠️ HMG **resells ~33 third-party items** (MSR/Therm-a-Rest/Sawyer/Nalgene/Vargo…) — kept with auto-type + buy-link but NOT deep-attributed (resold, may duplicate other catalog items); plus UltaMid inserts/bivies left as components.

### Brand-direct Shopify `products.json` (same toolchain as the others)
| Brand | Domain | Status |
|---|---|---|
| **Nashville Pack** | nashvillepack.com | ✅ **DONE** 2026-06-29. Feed polluted with Mageworx APO "service products" + MTO/Blem/One-Offs + accessories → archived 52, kept **5** (Cutaway [Fabric×Volume], Tiempo→Daypack, 2 liners [DCF/D50T], DCF Groundsheet). `curate-nashville.js`. |
| **Dandee Packs** | dandeepacks.com | ✅ **DONE** 2026-06-29. Cottage sundry shop → archived 23 junk, kept **20** gear+small-bags (15 attributed). `curate-dandee.js`. |
| **Hilltop Packs** | hilltoppacks.com | ✅ **DONE** 2026-07-01. 256 products, ~249 noise (stickers/custom/bargain prints/apparel/hardware/resold flashlights). Targeted create (NOT ingest) of **7** via `create-hilltop.js`: **Raven** (Fabric[Dyneema/Ecopak/X-Pac], 539/567/794g, per-variant mainFabric), **Dirty 30** (425g), **24Seven Trail** (Torso[16-22"], flat 425g), **Outlaw 35 Venom** (Torso[S/M/L/XL], flat 482g), **Roll Top Fanny Pack** (Hip Pack, Fabric[Ecopak/Dyneema] 85/62g), **RFP Pillow** (Size[S/L] 198/312g), **Bear Bag Throw Kit** (Other, 62g). ⚠ weights = published BASE/lightest config (feed grams were placeholders — Raven D&ECO both 680); torso packs have no per-size weight → flat. Direct (unmonetized) hilltoppacks.com offers. |
| **Darn Tough** | darntough.com | ❌ DECLINED (user doesn't want ~250 sock models). |
Process per brand: probe → `analyze-feed.js` → `ingest-shopify-catalog.js --brand --merchant-name [--exclude/--skip-food]` → `dedupe-ggg --group` → `ai-type-untyped --group` → curate (variants/weights/attrs, trim descriptions).

### 🔖 Researched, NOT YET imported (user deferred)
- **Nemo Equipment — ✅ DONE 2026-07-01** (`import-nemo.js` — dedicated importer, NOT the generic ingest, which collapses variants when the feed has no weights). Feed 95 → kept 51 backpacking (Tents/Sleeping Bags/Sleeping Pads/Pillows), dropped Carry Systems (=travel/everyday bags: Vantage/Double Haul duffels — NOT backpacking) + Camp Life/Furniture. Built variant structure from feed options minus Color (bags = Temperature×Length, tents = Capacity, pads = Size) + direct unmonetized nemoequipment.com offers. AI-typed (Sleeping Bag/Pad/Tent/Pillow, footprints→Ground Sheet, Airpin→Tent Stakes, Scalar→Tarp Shelter), replaced the **6 hand-entered Amazon-linked pads** (Tensor/Switchback), archived Vortex pump sack. **49 active — ✅ DONE FOR NOW (user, 2026-07-02):** structure/types/images/offers complete; weights left blank, user entering them manually in the admin variant editor as needed. ⚠️ **WEIGHTS: user-manual** (chose manual lookup over the unreliable scrape). page spec JSON has per-variant "Minimum Weight" but mixed with accessory/summary values → automated scrape unreliable (Tensor pad scraped ~142 g vs ~450 g real). `scrapeMinWeights()` kept in the script; a careful per-page-structure pass is needed. Nemo affiliate = AvantLink (user can't get on) → unmonetized links (fine).

### ❌ Skip — multi-brand resellers / no open feed
- **MEC** (mec.ca) — researched 2026-07-01. **Multi-brand Canadian co-op retailer** (NOT a single brand) + **Cloudflare** + `products.json` **403** (not Shopify/no open feed). Same "mud" category as GGG (removed) / Ultralight Outdoor Gear / Decathlon-as-reseller: importing = a huge multi-brand catalog with retailer (CAD) buy-links that duplicate brands we add directly. Only unique angle = MEC house-brand gear, but no feed → Cloudflare scrape, not worth it. **Skip** — brand-direct is strictly better.
- **Ultralight Outdoor Gear** (ultralightoutdoorgear.co.uk) — **BigCommerce + Cloudflare, NO open bulk feed** (BigCommerce API needs auth). Multi-brand UK reseller (Cumulus/Tarptent/etc.) = same "mud" category as GGG (which was deliberately removed). Importing = per-page Cloudflare scrape of reseller listings (GBP, UOG buy-links). **Skip** — brand-direct is strictly better. Only marginally useful as a back-door for no-feed brands, but heavy scrape. (Re-confirmed 2026-06-30; handoff already flagged it Cloudflare.)

### ⚠️ No open feed — need affiliate network or back-door (enterprise/Cloudflare/custom)
- **Macpac** (macpac.com.au) — researched 2026-07-01. AU own-brand (packs/apparel/sleeping bags). `products.json` **410 Gone** (deliberately disabled) + **Cloudflare**. No open feed → affiliate (AU networks) or manual copy-paste. Single-brand so cleaner than a reseller, but blocked like Osprey/Deuter.
- **Osprey** (Cloudflare challenge, SFCC), **Smartwool** (VF Corp, Cloudflare), **Rab** (enterprise) → need **AvantLink/Impact/Awin** product feed (most US outdoor brands live on AvantLink/Impact, NOT Awin). **Manual copy-paste entry also works** (user pastes specs/images/URL → build item like Bandit Lite).
- **Tarptent** (custom nginx cart, no products.json) → reseller back-door (outdoorline.eu) or scrape.
- **Cumulus** (Cloudflare-blocked, known) → reseller back-door (GGG/outdoorline) or affiliate.

### Sourcing reality
- **Amazon = NOT a catalog source.** Creators API / PA-API are per-ASIN lookup, rate-limited, ToS forbids permanent storage (code caches `amazonSnapshot` 24h + TTL). It's a **buy-link/price layer** for items you already have (attach an ASIN). Weights are shipping weights (wrong).
- **Awin** = bulk feeds but only per **approved merchant**; user has **Decathlon only** today. Apply for more, but big US brands are usually AvantLink/Impact.
- _User affiliate networks: Awin (Decathlon). AvantLink/Impact = TBD (the unlock for Osprey/Smartwool/Rab)._


## Session 2026-06-29 (afternoon) — accessory exclusion + first weights
- **Policy decisions SET** (see below): (1) hand-curate weights for real gear, blank ok for true no-weight accessories; (2) **exclude all accessories** (not needed for a gear-planning app) and keep a note of every exclusion.
- **mongodump insurance taken**: `server/backups/dump-treklist_local-20260629T094659/`.
- **Accessory exclusion committed**: archived **98** accessory items (pouches, spare parts, cordage/hardware, consumables, off-domain electronics/climbing, wallets/cash, junk-name orphans, collection pages) + **retyped 10** real-gear items (cups→Pot, rain kilts→Rain Pants, vests→Insulated Jacket, etc.). **19 borderline items kept** (gaiters, booties, pack cover, sit pads, microspikes, trowel, food bags…). Full list: `server/reports/exclusions-2026-06-29T07-58-07-933Z.txt`. Script: `curate-accessories.js` (dry-run default; reversible — archive = `isActive:false`).
- **First weights curated (hyberg)**: NIMBUS I–IV → Size M/L/XL variants; ZEFYR/VALGUS/RADA×2/Zolo×2/ExploMid I → single weights — all **verified by hand from each item's own spec-table text**. Also archived 3 hidden pack-pocket accessories (Hipbelt Pocket, Shoulder Strap Pocket, PILGER Lite). Script: `curate-weights-hyberg.js`.
- **Hyberg weights FINISHED via brand-site fetch** (method chosen by user). Pulled full untruncated spec tables from `https://hyberg.de/products/<handle>.json` `body_html` and read weights by hand. `curate-weights-hyberg2.js`: 18 single-Size-axis items (LONER Lite/SLUMBER/LONER APEX/SLUMBER APEX quilts, ARCON/ATTILA/ATTILA ULTRA packs, ZIP Bag) + 2 Material×Size packs (EGOIST LITE, ATTILA LITE). **AER PACK left blank** (empty description + grams=0, no published weight). Only Hyberg flags left: AER PACK + 2 Warm Booties (genuine Other).
- **LONER/SLUMBER over-split CONSOLIDATED** (`consolidate-hyberg-quilts.js`): 14 separate per-fill product items → **4 Fill×Size variant parents** (LONER Lite Down 12v, SLUMBER Down 9v, LONER APEX Synthetic 12v, SLUMBER APEX Synthetic 6v). ⚠️ Hyberg sells each fill as a SEPARATE product URL and **MerchantOffer has NO variantKey** (per-variant buy-links unsupported) → user chose single COLLECTION-page link per line (loner-lite / quilts / loner-apex / climashield-apex). 10 member items + their offers deleted (0 user refs, 0 orphan offers after). **NIMBUS (I–IV) + VALGUS left split** — same over-split pattern, not in the asked scope; consolidate them the same way if wanted.
- **zpacks quick-wins** (`curate-zpacks-and-category.js`): typed 8 borderline-kept items (DupleXL DISC→Tent, Carbon Staff→Trekking Poles, food bags→Dry Bag, Adotec→Bear Canister, Down Hood→Insulated Jacket, Whistle/Foam Sit Pad→Other); archived 6 mis-typed accessories that had escaped the sweep (tent-pole sacks/sling, mesh tent pocket, sleeping pad strap, Arc backpack belt). **Also fixed a retype bug**: catalog-wide backfilled `category` for 29 typed items that lacked it (category isn't auto-derived on `save()` — the earlier retypes + legacy Decathlon sleeping bags/boots).
- **STATE NOW: ~688 active / ~80 review flags** (was 261 at session start). Remaining: **zpacks 13 no-weight = CONFIGURABLE PACKS (deferred-by-design → user weight-override, see PACK WEIGHTS note in memory)**; **legacy/other ~49 no-weight = low-priority heterogeneous long-tail** (Smartwool/MH/Decathlon/Leki/guidebooks/permits — each a different site, no Shopify-.json shortcut; some weight-optional like Permit/Guidebook/Deodorant). Both are the natural next pass.

## ⚠️ WEIGHT-DATA TRUST ISSUE (found 2026-06-29 PM via user spot-check)
- User spot-checked 3 Hyberg pack weights (EVENT/AGUILA/AGUILA LITE) — **all 3 wrong**. Root cause: the **abandoned AI-extraction pass** (see WEIGHT/DESCRIPTION CLEANUP note in memory) left **placeholder/guessed weights** that got committed (e.g. AGUILA/AGUILA LITE/RINCO all == 400g; EVENT 470; SKINI 320). Audited ALL Hyberg items vs the brand site (`hyberg.de/products/<handle>.json` body_html) → **13 single weights were wrong, now FIXED** (`fix-hyberg-weights.js`). This session's hand-read weights + pre-existing VARIANT items (booties, dry/stuff bags, PAKKID) verified CORRECT.
- 🔴 **IMPLICATION: other brands curated in that same 2026-06-28 AI pass may also have bad weights** (Atom, Durston, Atelier, legacy). RE-VERIFY against brand sites before trusting any single-value weight from that era. The Shopify `/<handle>.json` body_html audit pattern in `fix-hyberg-weights.js`/`consolidate-hyberg-quilts.js` is reusable. Variant items built by hand from spec tables proved reliable; lone round numbers (400/500) are the smell.

## Where things stand
- **913 active catalog items**, 967 offers. This is the clean, focused catalog after removing GGG.
- **GGG was removed** (it was the mud: 1029 items / 376 review flags). Backed up at `server/backups/ggg-*.json` — re-addable in one command, or selectively by brand.
  - **▶ 2026-07-02 — GGG BACK IN THE GREEN (user): re-add SELECTIVELY** (NOT the full mud). Garage Grown Gear still has an open Shopify feed (`garagegrowngear.com/products.json`, now ~185 products / 78 vendors) and is our **back-door for no-feed cottage brands**. Import **by vendor**, only the brands we can't get direct + unique cottage brands; SKIP vendors we already import direct (Gossamer/HMG/Zpacks/Sea to Summit/LiteAF/Cowboy Camping) to avoid dup mud. **Unlocks (blocked/wanted):** Cumulus (4), Tarptent (1), Pa'lante Packs (1), Katabatic Gear (1), FarPointe Outdoor Gear (2) + unique cottage brands worth a look: TOAKS, Darn Tough, Klymit, Exped, Outdoor Vitals, SkyGOAT, Crystal Creek, MooseTrack, High Tail Designs, humangear, HydraPak. **Method:** `ingest-shopify-catalog.js --domain garagegrowngear.com --brand "<Vendor>" --merchant-name "Garage Grown Gear"` per vendor (GGG deep-links, unmonetized) → AI-type → curate. ⚠ weights: GGG feed grams unreliable (reseller) → verify per brand.
- **Prod `TrekList` is STALE** — 610 old items, none of this session's work (no variants, no new brands, no curation). Do **not** copy prod→local; it would erase everything.
- ⚠️ **local and prod `_id`s DIFFER** (verified). Users' `productId` refs point at prod `_id`s. → migration must be archive-and-readd (below), not an in-place `_id` update.
- ⚠️ **Manual curation is NOT replayable from scripts** — the Durston variant/weight work, consolidations, etc. exist only as data in local. local is the source of truth.

## The goal
A clean, consistent catalog, then migrate to prod. **Clean bar** = every active item has: an offer (already 100%) + valid `itemType` (specific or a genuine "Other") + `category` + a weight (or is a true no-weight accessory) + sane variants + no junk names.

## TWO POLICY DECISIONS — ✅ SET 2026-06-29
1. **No-weight items:** hand-curate the weight for real gear (from description/brand site); blank is OK for genuine no-weight accessories.
2. **"Other" accessories:** **exclude all** (archive) — not needed for a gear-planning app — and keep a note of what was excluded (`reports/exclusions-*.txt`).

## How to curate
- **Remove junk → Archive it** (admin catalog table "Archive" button = `isActive:false`). The migration only uploads **active** items, so archived = excluded from prod. Reversible. No need to hard-delete (local has no users).
- **Edit fields**: admin catalog table (`client/src/pages/AdminView.jsx`) / `GlobalItemEditModal.jsx`, or directly in the DB.
- **Add variants** (weight-VARYING only — color/torso-size that don't change weight should collapse): set on the CatalogItem:
  - `variantAxes: [{ name, values: [...] }]` (1+ axes)
  - `variants: [{ key: "<axis vals joined by ' / '>", options: { <axisName>: val, ... }, weightGrams }]`
  - `defaultVariantKey`, and `weightGrams` = the default variant's weight
  - Save with `doc.$locals.lenientAttributes = true`.
- **Weight convention used so far:** "Complete Pack" / "Complete Tent" (as-shipped). Many makers bury weights & size tables in the DESCRIPTION text (feed `grams`=0); descriptions are backfilled to full length (2000 chars).

## Tools (`server/src/scripts/`, dry-run default, `--db/--confirm` prod guard)
- **`consistency-report.js`** — re-run anytime; writes a per-source/per-reason review list to `server/reports/`. Buckets: NO-WEIGHT, UNTYPED, TYPE-BUT-NO-CATEGORY, SUSPICIOUS-WEIGHT, FLAT-VARIANTS, JUNK-NAME, OTHER, + POSSIBLE OVER-SPLIT (has false positives on distinct models).
- **`ai-type-untyped.js --group <slug> --commit`** — AI-types untyped items (object-keyed schema; reuses anthropicService).
- **`consolidate-variants.js`** — folds sibling size items into one variant item (explicit per-cluster configs; only removes unreferenced).
- **`dedupe-ggg.js --group <slug>`** — feed-vs-existing dedupe (existing wins; exact auto, fuzzy reported).
- **`backfill-descriptions.js`** — surgical description-only refresh from feeds (already run).
- **`ingest-shopify-catalog.js`** — the importer. Flags: `--platform woo`, `--brand`, `--merchant-name`, `--skip-food`, `--skip-brands`, `--exclude <regex>`, `--collapse-color`. Re-running MERGES by `itemGroupId` (refreshes facts, preserves curated `itemType`/`category`/`attributes`). ⚠️ Re-running would NOT overwrite manual variant/weight curation? — it refreshes `weightGrams`/`variants` from feed, so re-importing a curated brand CAN clobber hand weights. Be careful; prefer not to re-import curated brands.
- **`analyze-feed.js --domain <host>`** — pre-import analysis (classification/food/overlap).

## Known issues to work through (per source, from the report)
- **Hyberg (~40 to fix):** biggest gap = weightless quilts/packs (NIMBUS/LONER/SLUMBER quilts; AER/ARCON/ATTILA/ZEFYR/EGOIST packs; ExploMid tent). Weights are in their descriptions → extract by hand (or a careful per-item read; an AI extractor hallucinated earlier, so VERIFY against the text). **LONER Lite (4 fills) and SLUMBER (3 fills) are over-split** → consolidate into fill variants, but each line also splits Down vs APEX-synthetic (2 products each). PILGER weights ambiguous ("23g 31g", no labels). Warm Booties typed "Other".
- **Atelier (~12):** remove non-products → `CUSTOMIZATION FEES`, `Frais de personnalisation`, `Carte Cadeau`. Sakasek/Sakabouf packs show **40g = wrong** (fabric weight, not pack) → need real specs. A few French-named items untyped.
- **Atom (~9):** frames / padded hipbelt / foam mat / Roo (Zero Waste) genuinely weightless (feed `grams`=0 — hand-curate or accept). Rest of "Other" are real accessories. `X-Dome Pro 1+`-equivalent: none here.
- **Durston:** clean. `X-Dome Pro 1+` weightless (upcoming/pre-release — leave). Spare parts/stickers = "Other" (archive the stickers/logo/customization junk). Tents + packs fully modeled.
- **Zpacks (~95):** mostly genuine "Other" accessories + some no-weight. Solo Quilt / Classic Sleeping Bag = 21-variant parents (good).
- **Legacy/other (~78):** your CLEANEST set (95% typed) — most flags are genuine "Other" accessories. Low priority.
- **NOTE:** earlier-flagged consolidation clusters Cumulus Aerial / Katabatic Flex / Vargo BOT / Adventure Medical Kit were all GGG-sourced → **gone with GGG**. Only Hyberg LONER/SLUMBER remains.

## Atelier DONE — ✅ 2026-06-29
- French artisan (WooCommerce). 12 → **8** clean items: Classique + Hybride consolidated into config items (Personnalisé 600g / All Around 1000g); deduped Sac Banane vs BANANA HIP PACK (EN twin); archived belt-pocket accessory; **cleared bogus Sakabouf/Sakasek weights** (feed value = fabric weight, real pack weight unpublished); basic attrs + brief honest descriptions (feed descriptions are empty "AVANT DE PERSONNALISER //" placeholders). Per-variant weights for size/fabric not published. `curate-atelier.js`.

## Atom Packs DONE — ✅ 2026-06-29
- Consolidated the per-config items into **5 variant pack models** (`consolidate-atom-packs.js`): The Atom (RE30/RE40/UL35/Custom), Prospector (EP50/EP60/Custom), Pulse (EP40/EP50/Custom), Notch (EP40/EP50/Custom), Nanu (RE25/X25/Custom). Variant axis "Version"; each variant keeps its published **medium-torso weight** (user overrides for their torso) + **per-variant attributes** (volumeLiters + mainFabric swap on selection). Parent offers → `<model>-custom` builder pages (Nanu has no builder → its product page). Redundant config items **archived** (not deleted — they had local gear refs).
- Earlier (`curate-atom.js`): Nanu weight 600→510, Thinny blank→Size variants (87/116/145g), Roo RE hip-pack attrs, Pack Liner attrs. The Atom stray custom-build weight 1336g fixed via the consolidation.
- **Accessories kept (4):** un-archived **The Hipbelt Pocket**; kept Roo RE (1.5/2.5L), Pack Liner, The Thinny. All other Atom accessories/frames/old-Roo-dupes/test-copy/Mo-factory-2nd stay archived. **Active Atom = 9** (5 models + 4 accessories).

## Durston DONE — ✅ 2026-06-29
- Durston was already well-modeled (variants + per-variant weights survived from a prior session — Kakwa Fabric×Torso 770–860g, Wapta Torso×WaistBelt 385–540g, X-Mid Pro Floor×Stakes, X-Dome Interior×PoleSet, all correct vs the handoff). **Gap was attributes — now filled** for all 13 active items from durstongear.com specs (WebFetch + cross-check). Scripts: `curate-durston-attrs-tents.js`, `curate-durston-attrs-rest.js`.
- 8 tents: capacity/season/tentType/wall/materials/dims; **per-variant** floor material on X-Mid Pro (Woven=silnylon / Dyneema=DCF) and floor+pole material on X-Dome (Interior + Pole Set axes). X-Dome Pro 1+ left minimal (pre-release, no published specs).
- 3 packs (brand "Durston Gear", no itemGroupId — match by name+brand): Wapta 30 (ALUULA, removable padded belt), Kakwa 40/55 (internal frame, **per-variant mainFabric** Ultra 200X / UltraGrid). Iceline poles: weight corrected **134 g (per pole) → 268 g (the pair, as sold)** + full pole attrs. Groundsheets: material.
- Brand consistent (all "Durston Gear"). Descriptions already short (148–460 chars) so NOT trimmed (no 2000-char dumps like Hyberg). 13/13 have attributes, brand, images intact.

## ⚠️ INCIDENT + RECOVERY (2026-06-29 PM) — read this
- A `trim-hyberg-descriptions.js` early version loaded docs with `.select("name description")` then `.save()`. The CatalogItem **pre-save hook re-normalizes every field from `this.*`**, so unloaded fields (brand/brandLC/category/subcategory/imageUrls/itemGroupId/tags…) were written back as blanks → wiped on the 34 trimmed Hyberg items. **NEVER call `.save()` on a doc loaded with `.select(...)` on this model** — use `collection.updateOne({$set})` (now fixed in the trim script). See memory [[gotcha-select-save-wipes-fields]].
- Recovery: restored `catalogitems` + `merchantoffers` from the morning mongodump (clean base), then **replayed all committed scripts in order** to rebuild. ⚠️ During recovery a `mongorestore --drop --nsTo treklist_restore` ALSO clobbered `treklist_local.catalogitems` (the nsTo didn't protect the source) — that's WHY a full replay was needed; be careful with mongorestore ns-remap + `--drop`.
- **Fresh backup of the recovered/curated state: `server/backups/dump-treklist_local-RECOVERED-20260629T120025/`** (this is now the newest good snapshot — prefer it over the 09:46 one).
- Full session is reproducible from git: run `curate-accessories` → `curate-weights-hyberg` → `curate-weights-hyberg2` → `consolidate-hyberg-quilts` → `curate-zpacks-and-category` → `fix-hyberg-weights` → `curate-bandit-pakkid` → `curate-hyberg-attrs-{backpacks,quilts,sleepingbags,rest}` → `curate-hyberg-misc` → `consolidate-hyberg-nimbus-valgus` → `trim-hyberg-descriptions` (all `--commit`).

## Hyberg ATTRIBUTES filled — ✅ 2026-06-29
- Structured `attributes` filled on **40/43** active Hyberg items, hand-read from hyberg.de spec tables (no AI), modelled on Bandit Lite. Scripts: `curate-hyberg-attrs-{backpacks,quilts,sleepingbags,rest}.js`. Backpacks (volume/gender/frame/hipbelt/water/fabric/torso/load/hydration); Quilts (insulation/temp/fillPower/footbox — **consolidated quilts carry PER-FILL tempRatingC + fillWeightG/Climashield level via variant.attributes**); VALGUS bags; ExploMid tents + Zolo/SKINI tarps; dry/stuff bags; RADA hip packs; Ti mug/spoon. Per-variant material on EGOIST LITE/ATTILA LITE/PAKKID. Retypes: RINCO/ZEFYR → Daypack (18L < Backpack min 20L); "Stake Bag" Tent Stakes → Dry Bag/Stuff Sack (mis-typed). Unfillable: AER PACK (no brand desc), 2 Warm Booties (itemType "Other" has no schema).
- **Descriptions TRIMMED** (`trim-hyberg-descriptions.js`): all 34 active Hyberg descriptions cut from ~2000 chars to the first 1–2 sentences of the marketing intro (specs now live in attributes). Deterministic (real prose, no AI). ⚠️ Reversible but `backfill-descriptions.js` would RE-EXPAND them from the feed — don't re-run backfill on Hyberg unless you want the long text back.
- **NIMBUS + VALGUS consolidated** (`consolidate-hyberg-nimbus-valgus.js`): NIMBUS I–IV → NIMBUS Down Quilt (Fill×Size, per-Fill temp/fill-weight); VALGUS I/II/III → VALGUS Down Sleeping Bag (Fill, per-Fill temp). **Archived dated dupes**: Warm Booties (2024), AGUILA LITE (2025), ATTILA ULTRA (2024 version). Active Hyberg 43 → **35**.
- 🔴 Same brand-spec attribute-fill is a candidate for Atom/Durston/Atelier next.

## Per-variant attributes — ✅ BUILT 2026-06-29
- Variant sub-schema now has an optional `attributes` map (`catalogItem.js`). Both modals (`GlobalItemEditModal.jsx`, `CatalogItemPreviewModal.jsx`) merge `selectedVariant.attributes` OVER the base `attributes` in their specs memo, so selecting a variant swaps those fields. First use: **Bandit Lite** (Material: Aluula 410g / Dyneema 420g, `mainFabric` differs per material) + **PAKKID** (Material×Size: Dyneema 30/33, TX50Ultra 38/41). Script: `curate-bandit-pakkid.js`. Catalog route already selects `variants` wholesale so attributes reach the client. **Add-from-catalog now denormalizes the CHOSEN variant's attributes** onto the owned GlobalItem (`globalItems.js` bulk-add merges `variant.attributes` over base, alongside variant weight + variantKey). So picking Dyneema then adding stores Dyneema fabric+weight. ⚠️ The edit-existing PATCH path saves variantKey+weight but NOT re-resolved attributes — display stays correct (re-resolves from variantKey + catalog variants), but stored `attributes` can go stale if a user CHANGES variant on an already-owned item. Wire the same merge into the update route if that matters. Bandit Lite also got 8 brand photos (was 1 custom).

## Migration plan (when the catalog is clean) — archive-and-readd
1. `mongodump TrekList` (backup; rollback = `mongorestore --drop`).
2. Deploy code `dev → main` (variant UI + importer; inert on current data; also ships waiting F2 JWT fix).
3. Archive ALL current prod catalog items: `updateMany({}, {$set:{isActive:false}})` — preserves user `productId` refs (their gear is denormalized; refs resolve to hidden items; offers untouched → nothing breaks; worst case = a hidden item).
4. Insert the local **active** catalog + its MerchantOffers into prod, keeping local `_id`s (no collision — prod ids differ).
5. Verify: active catalog = the new set; a sample existing user's gear still renders + has its buy button; 0 orphan offers.
   - Tradeoffs (acceptable): existing users don't retroactively get variants on already-owned items; ~610 archived ghosts linger.
6. **GlobalItem index migration (REQUIRED on deploy):** `node src/scripts/migrate-globalitem-variant-index.js --db TrekList --confirm TrekList` — drops the old `{owner,productId}` unique + stale `_status` orphan indexes and creates `{owner,productId,variantKey}`, so users can own multiple variants of one item (fixed 2026-07-03). Also run `backfill-variant-links.js --db TrekList --confirm TrekList` to point owned items' `affiliate.deepLink` at their per-variant URL.

## Immediate insurance to take
- `mongodump treklist_local` (the whole curated catalog only exists in this one DB).
- Push the `dev` commits (code is local-only on this machine).
